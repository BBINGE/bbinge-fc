# 에디터 명세 v1.0 — 삥이FC 발행 시스템

확정일: 2026-07-28 · 운영자 확정 조건 반영

## 0. 이 문서의 위상

이것은 삥이FC의 **최우선 개발 과제**다. 게임·GOAT·디자인은 애드센스 유입용 기반이고, 사이트의 존재 이유는 운영자만 쓸 수 있는 글(축구 지식 × 세계사·철학·인문학 × 문장력 × 대체불가 주제 × 디자인·가독성 집요함)이다. 따라서 "운영자가 글쓰기에 피로를 느끼지 않는 것"이 이 시스템의 유일한 성공 기준이다. 기능이 많아도 쓰기 불편하면 실패다.

## 1. 목표와 반드시 피할 것

- 목표: 네이버 스마트에디터 원(ONE) 수준의 편안함. 마크다운 문법이 화면에 보이지 않는 위지윅. "+" 버튼으로 미디어 삽입, 우측 상단 발행 버튼으로 끝.
- 반드시 피할 것: 개발자용 CMS(현재 Sveltia/`public/admin`)의 마크다운 노출 방식. 운영자는 개발자가 아니며, 이미 글쓰기 자체의 피로가 크다. 어설픈 자체 구현으로 "스마트에디터보다 불편한" 결과를 내면 안 된다.
- 구현 방침: 검증된 오픈소스 위지윅 에디터(TipTap/ProseMirror 계열 등)를 기반으로 삼는다. 서식·표·인용 같은 기본기는 이미 완성된 것을 얹고, 우리는 이미지 저장·발행 흐름만 연결한다. 위지윅 코어를 처음부터 자작하지 않는다.

## 2. 운영자가 실제로 쓰는 기능 (스마트에디터 원 기준)

포함(전부 구현 대상):
- 미디어: 사진(업로드), 동영상(유튜브 임베드), 파일 첨부
- 구조: 인용구, 구분선, 링크, 표, 수식, 장소
- 서식: 제목/본문 스타일, 폰트, 글자 크기, 굵게·기울임·밑줄·취소선, 글자색, 형광펜, 정렬(좌·중·우), 리스트, 위/아래 첨자, 링크, 맞춤법

제외(운영자 미사용): 마이박스, 스티커, 일정, 소스코드, 내돈내산, 글감.

## 3. 확정된 제약 (운영자 결정, 2026-07-28)

- **영상**: 유튜브 임베드로만 처리한다. 영상 파일 직접 업로드 없음 → 저장·비용 부담 최소화.
- **이미지**: 포토샵·프리미어에서 완성한 카드뉴스 이미지/GIF를 업로드한다. 에디터 안에서 이미지·영상 편집 기능은 불필요(외부에서 완성해 옴). 필요한 것은 "편하게 얹기"뿐. GIF 지원 필수.
- **기존 티스토리 글 이전 없음**: 새 글부터 시작. 마이그레이션 작업 불필요.
- **비공개 단일 사용자**: 운영자 전용 관리자. 다중 사용자·권한 관리 불필요 → 로그인 단순화 가능.
- **카테고리 자유 커스텀**: 카테고리를 코드가 아니라 데이터로 관리해, 운영자가 글을 쓰면서 새 카테고리를 추가·수정·삭제할 수 있어야 한다. (현재 축세·축행·축겜·축디는 코드 하드코딩 → 데이터화 필요)

## 4. 이미지 저장 (해결해야 할 핵심 인프라)

- 현재 사이트는 정적이라 이미지 저장소가 없다. 카드뉴스 대작이 쌓이면 GitHub에 넣을 수 없다(저장소 비대화·빌드 지연).
- 대안: **Cloudflare R2**(오브젝트 스토리지). 무료 티어 월 10GB 저장 + 이그레스(전송) 무료. D1과 동일하게 클라우드플레어 무료 티어 범위이며 카드 미등록 시 과금 불가 → 운영자의 한도·비용 우려 해소.
- 업로드 흐름: 에디터에서 이미지 선택 → Pages Function이 R2에 업로드 → 반환된 URL을 본문에 삽입. GIF 포함.

## 5. 발행 흐름

- 저장: 작성 중 자동 저장(초안). 1~2만 자 장문 작성 중 유실 방지 필수.
- 발행: 완성된 글을 사이트 콘텐츠로 확정. 스마트에디터처럼 발행 패널에서 카테고리 선택, 태그, 공개/비공개, 예약 발행(미래 시각) 지원.
- 저장 위치: 글 데이터(본문 HTML/JSON + 메타)를 어디에 둘지 결정 필요. 두 갈래 — (a) 기존 방식대로 `src/content/articles/*.md`로 GitHub 커밋 후 재빌드(정적 유지, 단순), (b) D1 등 DB에 저장하고 동적 렌더(즉시 반영, 복잡). v1은 (a)를 우선 검토하되, 자동 저장·예약 발행 요구와의 정합성을 구현 단계에서 판단한다.

## 6. 구현 단계 제안

1. 오픈소스 위지윅 에디터 선정·이식, 운영자 사용 기능만 활성화, 380px 포함 반응형
2. R2 이미지·GIF 업로드 연결
3. 유튜브 임베드 블록
4. 카테고리 데이터화 + 에디터 내 카테고리 추가 UI
5. 자동 저장(초안) + 발행 패널(카테고리·태그·공개설정·예약)
6. 발행 → 사이트 반영 흐름, 3구간 검증

## 7. 검증 기준

- 운영자가 실제로 1~2만 자 글 + 이미지 여러 장 + 유튜브 임베드 + 표·인용을 넣어 발행하는 전 과정을 피로 없이 완료할 수 있는가. (이 한 문장이 최종 합격 기준)
- 마크다운 문법이 화면에 노출되지 않는가.
- 자동 저장이 장문에서 안정적인가.
- 이미지 저장이 무료 티어 범위에서 동작하는가.

## 8. 미결 · 착수 전 결정할 것

- 글 저장 방식 (a)정적 커밋 vs (b)DB 동적 — §5 참조. 자동저장/예약발행 요구를 고려해 구현 세션 초입에 확정.
- 로그인 방식 (기존 GitHub OAuth 재사용 vs 단순 비밀번호). 비공개 단일 사용자이므로 과하지 않게.
- 이 시스템 완성 후 기존 Sveltia CMS(`public/admin`) 유지 여부 — 대체되면 제거 검토.
