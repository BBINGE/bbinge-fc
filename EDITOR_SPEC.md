# 에디터 명세 v1.1 — 삥이FC 발행 시스템

확정일: 2026-07-28 · 운영자 확정 조건 및 착수 결정 반영

## 0. 이 문서의 위상

이것은 삥이FC의 **최우선 개발 과제**다. 게임·GOAT·디자인은 애드센스 유입용 기반이고, 사이트의 존재 이유는 운영자만 쓸 수 있는 글(축구 지식 × 세계사·철학·인문학 × 문장력 × 대체불가 주제 × 디자인·가독성 집요함)이다. 따라서 "운영자가 장문 글쓰기에 피로를 느끼지 않는 것"이 이 시스템의 유일한 성공 기준이다. 기능이 많아도 쓰기 불편하면 실패다.

## 1. 목표와 반드시 피할 것

- 목표: 네이버 스마트에디터 원(ONE) 수준의 편안함. 마크다운 문법이 화면에 보이지 않는 위지윅. "+" 버튼으로 미디어 삽입, 우측 상단 발행 버튼으로 끝.
- 반드시 피할 것: 개발자용 CMS(현재 Sveltia/`public/admin`)의 마크다운 노출 방식. 운영자는 개발자가 아니며, 이미 글쓰기 자체의 피로가 크다. 어설픈 자체 구현으로 "스마트에디터보다 불편한" 결과를 내면 안 된다.
- 구현 방침: 검증된 오픈소스 위지윅 에디터(TipTap/ProseMirror 계열 권장)를 기반으로 삼는다. 서식·표·인용 같은 기본기는 이미 완성된 것을 얹고, 우리는 이미지 저장·발행 흐름만 연결한다. 위지윅 코어를 처음부터 자작하지 않는다.

## 2. 운영자가 실제로 쓰는 기능 (스마트에디터 원 기준)

포함(전부 구현 대상):
- 미디어: 사진(업로드), 동영상(유튜브 임베드), 파일 첨부
- 구조: 인용구, 구분선, 링크, 표, 수식, 장소
- 서식: 제목/본문 스타일, 폰트, 글자 크기, 굵게·기울임·밑줄·취소선, 글자색, 형광펜, 정렬(좌·중·우), 리스트, 위/아래 첨자, 링크, 맞춤법

제외(운영자 미사용): 마이박스, 스티커, 일정, 소스코드, 내돈내산, 글감.

## 3. 확정된 제약 (운영자 결정, 2026-07-28)

- **영상**: 유튜브 임베드로만 처리한다. 영상 파일 직접 업로드 없음 → 저장·비용 부담 최소화.
- **이미지**: 포토샵·프리미어에서 완성한 카드뉴스 이미지/GIF를 업로드한다. 에디터 안에서 이미지·영상 편집 기능은 불필요(외부에서 완성해 옴). 필요한 것은 "편하게 얹기"뿐. GIF 지원 필수.
- **기존 티스토리 글 이전 없음**: 새 글부터 시작.
- **비공개 단일 사용자**: 운영자 전용 관리자. 다중 사용자·권한 관리 불필요.
- **카테고리 자유 커스텀**: 카테고리를 코드가 아니라 데이터로 관리해, 운영자가 글을 쓰면서 새 카테고리를 추가·수정·삭제할 수 있어야 한다. (현재 축세·축행·축겜·축디는 코드 하드코딩 → 데이터화 필요)

## 4. 착수 결정 (운영자 확정, 2026-07-28) — 이전 미결사항 확정본

### 4-1. 글 저장 방식 → **정적 마크다운 파일 + GitHub 커밋**

- 발행 시 글을 `src/content/articles/{slug}.md`(frontmatter + 본문)로 변환해 GitHub `main`에 커밋 → Cloudflare Pages 재빌드로 사이트 반영.
- **핵심 근거(운영자 가치관)**: 글이 운영자 소유의 GitHub에 순수 텍스트 파일로 영구히 남는다. 사이트·호스팅·AI가 사라져도 글은 운영자 손에 파일로 존재(메모장으로도 열림). "내 자산으로 축적, AI 없이 자생" 원칙을 기술로 구현한 것. 티스토리(카카오 서버 종속)의 정반대.
- **중요**: 운영자가 마주하는 화면에는 마크다운이 절대 노출되지 않는다. 위지윅으로 쓰고, 저장 시 뒤에서 자동 변환될 뿐이다. "마크다운 저장"은 보관 형식이지 작성 경험이 아니다.
- DB(D1) 방식은 채택하지 않음: 글이 DB에 갇혀 운영자가 직접 못 꺼내며 종속이 생김.

### 4-2. 로그인 방식 → **기존 GitHub 로그인 재사용**

- 현재 `public/admin`이 이미 GitHub OAuth로 동작. 이를 재사용한다. 새 비밀번호 체계를 만들지 않음(코드 노출 위험·관리 부담 회피). 비공개 단일 사용자이며, 글을 GitHub에 커밋하는 4-1 구조와 자연스럽게 맞물린다.

### 4-3. 자동 저장(초안)

- 장문(1~2만 자) 유실 방지를 위해 작성 중 자동 저장. 초안은 브라우저 로컬(localStorage/IndexedDB)에 임시 보관하고, **발행을 눌렀을 때만** GitHub에 파일로 커밋한다. 재빌드는 발행 시 1회만 발생 → 빌드 낭비 없음.

### 4-4. 예약 발행

- 미래 시각 지정 시, 기존 예약 배포 워크플로(`.github/workflows/scheduled-deploy.yml`, 현재 6시간 주기)를 활용해 시각 도달 후 노출. frontmatter의 `pubDate`와 draft 플래그로 제어.

## 5. 이미지 저장 인프라

- 정적 사이트라 이미지 저장소가 없다. 카드뉴스 대작이 쌓이면 GitHub에 직접 넣을 수 없다(저장소 비대화·빌드 지연).
- **Cloudflare R2** 사용: 무료 티어 월 10GB 저장 + 이그레스 무료. D1과 동일하게 무료 범위이며 카드 미등록 시 과금 불가.
- 업로드 흐름: 에디터에서 이미지/GIF 선택 → Pages Function이 R2 업로드 → 반환 URL을 본문에 삽입.

## 6. 구현 단계

1. 오픈소스 위지윅 에디터 이식, §2 기능만 활성화, 380px 포함 반응형. 마크다운 미노출 확인.
2. R2 이미지·GIF 업로드 연결
3. 유튜브 임베드 블록
4. 카테고리 데이터화 + 에디터 내 카테고리 추가 UI
5. 자동 저장(초안, 로컬) + 발행 패널(카테고리·태그·공개설정·예약)
6. 발행 → 마크다운 변환 → GitHub 커밋 → 사이트 반영 흐름, 3구간 검증

각 단계는 독립 커밋. 한 세션에 전부 하려 하지 말고 단계별로 진행하며 운영자가 중간중간 확인한다.

## 7. 검증 기준 (최종 합격선)

운영자가 실제로 1~2만 자 글 + 이미지 여러 장 + 유튜브 임베드 + 표·인용을 넣어 발행하는 전 과정을 **피로 없이** 완료할 수 있는가. 이 한 문장이 유일한 최종 기준이다. 부수 조건: 마크다운 문법 미노출, 장문 자동저장 안정성, 이미지 저장 무료 범위 동작, 발행 후 글이 `.md` 파일로 GitHub에 남는지 확인.

## 8. 완성 후 정리

- 이 시스템이 안정화되면 기존 Sveltia CMS(`public/admin`)는 역할이 대체되므로 제거를 검토한다(로그인 흐름은 재사용하되 편집 UI는 신 에디터로 일원화).
