# BBinge FC — Codex/AI 작업 규칙

## 0. 최우선 운영 계약 — 사용자가 매번 다시 교정하지 않게 한다

이 절은 다른 세부 절차보다 먼저 적용한다. 새 세션은 사용자가 이미 여러 차례 확정한 아래 판단을 다시 질문하거나 일반론으로 되돌리지 않는다.

- 사용자는 개발 도구 조작에는 단계 안내가 필요할 수 있지만, 콘텐츠·브랜드·마케팅·검색·편집 기획의 초보가 아니다. `개발 초보`를 사고 수준이나 독자 수준이 낮다는 뜻으로 확대하지 않는다.
- 존댓말은 문장을 유치하게 쓰라는 뜻이 아니다. 자명한 사실을 풀어 말하거나(`영상에서는 옷이 움직여요`), 초등학생식 결론·가짜 친근함·상투적인 연예기사 문장을 만들지 않는다.
- 삥이FC의 카테고리는 같은 편집 공식을 공유하지 않는다. `축디`는 패션 매거진처럼 인물·옷·장면을 통해 **욕망과 구매 가능성**을 판다. `축행`은 운영자의 1인칭 여행기가 아니라 독자가 동행인을 설득할 증거와 도시·경기장·숙박·동선을 대신 모아 주는 편집물이다. `축술`은 경기와 전술의 구조를 읽게 하고, `축떡`은 규칙·판정·논쟁을 쉽게 설명하되 독자의 지성을 낮추지 않는다. `축세`는 사건과 세계사의 관계를 논증한다.
- 공개 제목은 구독자 팬덤에 기대는 소설식 제목보다 한국어 검색어와 대상이 즉시 보이는 제목을 우선한다. 본문과 화면은 검색 유입 뒤 독자의 욕망·호기심·체류를 키우도록 편집한다.
- 사진·영상·CSS 모션은 장식이 아니라 편집 자산이다. 역할이 다른 강한 시각 자료를 충분히 사용하며, 구체적 A·B등급 신호 없이 `안전`을 이유로 사진 수·매력·화질·인물성을 줄이지 않는다.
- `일반인`은 비전문 자격을 뜻할 뿐 낮은 이해도를 뜻하지 않는다. 사용자의 선택과 기획을 분석할 때 초보자 교육, 자격 심사, 과도한 칭찬이나 도덕적 훈계로 바꾸지 않는다.
- 운영자가 이미 승인한 카피·디자인·사진과 직접 작성한 본문을 보존한다. 지정된 결함만 고치고, 작은 수정 요청을 원고 전체 재작성이나 새 기획으로 확대하지 않는다.
- 논의·질문·첨부는 구현 승인으로 간주하지 않는다. 반대로 `바꿔`, `올려`, `발행해`처럼 명시한 작업은 불필요한 재확인 없이 요청 범위 안에서 완료한다.
- 저작권·AdSense·이미지·영상 판단은 `EDITORIAL_RISK_GATE.md`의 A·B·C 증거 등급을 적용한다. C등급 배경 가능성을 문제로 승격하거나 운영자에게 매체의 가치와 사진·영상의 필요성을 다시 해명하게 하지 않는다.
- 같은 세션에서 한 번 확인한 영구 문서와 현재 상태를 매 미세 수정마다 다시 읽지 않는다. 세션 압축 뒤에도 제공된 작업 요약을 이어받고, 실제 변경 가능성이 있는 대상만 다시 확인한다.

## 1. 새 세션 시작 — 고정 원칙과 현재 상태를 분리한다

새 채팅·새 에이전트 실행에서만 다음 **핵심 문서**를 순서대로 처음부터 끝까지 읽는다.

1. `BBINGE_FC_BRIEF.md` — 프로젝트의 단일 설계 기준 (브랜드·구조·성능 원칙)
2. `CLAUDE.md` — 장기 운영 규칙
3. `EDITORIAL_WRITING_RULES.md` — 전 카테고리 문체·소제목·원어 병기·공개 본문 규칙
4. `HANDOFF.md`의 `CURRENT-BRIEF:START`부터 `CURRENT-BRIEF:END`까지 — 현재 구현 상태·최근 발행·다음 작업

`HANDOFF.md`의 나머지는 보존용 작업 연대기다. 새 세션마다 1,800줄 전체를 읽지 않는다. 실제 작업과 관련된 파일명·카테고리·기능·과거 결정이 필요할 때 `rg`로 해당 항목만 찾아 읽는다.

아래 문서는 작업 종류에 해당할 때만 처음부터 끝까지 추가로 읽는다.

- `EDITORIAL_RISK_GATE.md` + `EDITORIAL_ASSET_POLICY.md`: 새 콘텐츠의 사진·영상·썸네일을 고르거나 수정할 때, 또는 저작권·AdSense·출처·저가치·사이트 위험을 묻거나 감사할 때
- `PLAYER_ARCHIVE_RULES.md`: 선수 인물 아카이브를 조사·작성·수정·발행할 때

이미 같은 세션에서 읽은 문서는 파일이 변경되지 않았다면 다시 읽지 않는다.

저작권, 이미지, 영상, 출처, AdSense, 저가치 콘텐츠, 사이트 위험을 묻거나 감사할 때는 답변 전에 `EDITORIAL_RISK_GATE.md`의 A·B·C 등급을 실제 대상에 적용한다. 구체적 외부 신호가 없는 C등급 가능성을 오류나 수정 과제로 보고하지 않으며, 운영자에게 사진·영상의 필요성과 사이트의 가치를 다시 해명하게 하지 않는다.

## 2. 작업 시작

```powershell
git fetch origin
git status --short --branch
git log -10 --oneline
npm run validate:risk-gate
```

- 기준 브랜치는 `main`이다.
- 원격보다 뒤처졌고 로컬 변경이 없다면 `git pull --ff-only origin main`으로 최신화한다.
- 사용자의 변경이나 출처가 불분명한 변경은 덮어쓰거나 삭제하지 않는다.
- 공개 저장소이므로 비밀번호, 토큰, API 키, OAuth 시크릿, 배포 훅 URL을 파일에 기록하지 않는다. 비밀값은 Cloudflare Pages 환경변수와 GitHub Secrets에만 존재한다.
- 저작권·AdSense·자산 판단처럼 `EDITORIAL_RISK_GATE.md`가 필요한 작업에서는 시작 검사가 통과해도 문서 읽기를 생략하지 않는다. 검사는 필수 규약이 진입점과 빌드에서 떨어져 나가지 않았는지 확인하는 회귀 방지 장치이지, 문맥 판단을 대신하지 않는다.

새 세션의 첫 실제 저장소 작업에서만 위 시작 검사를 실행한다. 같은 세션의 연속된 미세 수정마다 `fetch/status/log`와 위험 게이트를 반복하지 않는다. 다만 push 뒤 원격 상태가 바뀌었거나 사용자가 다른 PC·다른 작업의 변경 가능성을 알렸다면 다시 확인한다.

## 3. 작업 크기 분류 — 검수의 깊이는 위험에 맞춘다

작업 전에 아래 셋 중 하나로 분류하고, 불필요하게 더 큰 경로로 올리지 않는다.

### A. 미세 수정 빠른 경로

다음을 **모두** 만족할 때만 사용한다.

- 이미 발행된 Markdown 글 한 파일의 오탈자·소제목·캡션·짧은 문구 수정
- 변경 범위가 대체로 5줄 이하
- frontmatter, 날짜, 인명·기록·사실 주장, 출처·링크, 이미지·영상·임베드, HTML·CSS·JS·컴포넌트·데이터 구조를 건드리지 않음

절차:

1. 대상 줄과 앞뒤 문맥, 현재 Git diff만 확인한다.
2. 요청받은 문구만 수정한다.
3. `npm run build`를 **한 번** 실행한다. 빌드에 위험 게이트·글쓰기 자체 시험·전체 원고 검수·선수 검수가 이미 포함되므로 같은 검사를 먼저 중복 실행하지 않는다.
4. 제목·소제목처럼 줄바꿈에 영향을 주는 문구만 해당 위치를 380px와 1440px에서 확인한다. CSS·구조 변경이 없으면 768px 전체 순회와 전 페이지 검수는 생략한다.
5. 빌드가 자동 갱신한 `src/data/today-football.json`이 이번 요청과 무관하면 내용 해시를 확인하고 스테이징에서 제외한다.
6. 기존 공개 글을 `바꿔` 달라는 요청이면 대상 파일만 커밋·push하고 배포 SHA와 새 문구를 한 번 확인한다.

초안 문구가 화면에서 실패해 다시 고친 경우 최종 문구로 빌드와 대상 화면 확인을 한 번 더 하는 것은 허용한다. 실패하지 않은 검사를 습관적으로 반복하지 않는다.

### B. 일반 편집 경로

기존 글의 여러 문단, 사실·출처, 이미지, 영상, HTML 편집 모듈을 바꾸는 작업이다. 관련 영구 문서만 추가로 읽고, 글쓰기/선수/위험 검수 중 실제 범위에 해당하는 검사를 실행한 뒤 `npm run build`를 통과시킨다. 화면은 변경이 영향을 주는 380·768·1440px와 대상 기능만 확인한다.

### C. 전체 발행·구조 변경 경로

새 글, 새 페이지, CSS·컴포넌트·CMS·데이터·빌드·배포 구조 변경이다. 관련 규약 전체, 개별 검수, 전체 빌드, PC·태블릿·모바일 검증, 커밋·push, 실제 배포 확인을 모두 수행한다.

논의·기획·진단처럼 쓰기 승인이 없는 요청은 읽기 전용이다. 빌드·화면 순회·커밋·배포를 시작하지 않는다.

## 4. 작업 중

- 사용자는 개발 도구 조작의 초보다. 사용자가 직접 해야 하는 일이 있으면 프로그램 이름, 클릭 위치, 실행 명령을 순서대로 설명하되 콘텐츠·기획 판단까지 초보자 수준으로 낮추지 않는다.
- 브리프의 기술 스택(Astro + Cloudflare Pages, 백엔드 없음, Tailwind)을 임의로 바꾸지 않는다.
- 확정된 브랜드 문구, 카테고리 구조, 운영자가 작성한 글 본문을 요청 없이 수정하지 않는다.
- 논의·질문은 구현 승인으로 간주하지 않는다.
- 저작권·AdSense 감사를 수행하거나 관련 운영 문서를 바꾼 작업은 `npm run validate:risk-gate`를 통과시킨다.
- 새 글을 쓰거나 기존 원고·소제목·캡션을 수정할 때는 `EDITORIAL_WRITING_RULES.md`를 적용한다. A 경로는 중복 검사를 피하고 `npm run build` 한 번으로 포함된 검수를 실행하며, B·C 경로는 범위에 맞는 개별 검사와 빌드를 통과시킨다.
- CMS(`public/admin/`, `functions/`)를 수정할 때는 Sveltia CMS 설정과 GitHub OAuth 흐름의 호환성을 함께 확인한다.
- 선수 인물 글을 조사·수정·발행할 때는 `PLAYER_ARCHIVE_RULES.md`의 체크리스트를 생략하지 않는다.
- 선수 인물 글의 개인 수상·기록·등번호를 수정하기 전에는 카푸와 카카의 같은 섹션을 실제 파일에서 대조한다. `통일`을 새 해설·새 항목을 늘리라는 뜻으로 재해석하지 않는다.
- 검수 실패는 발행 중단 신호다. 오류 문구에서 원인을 찾고 해당 원고나 코드를 수정한 뒤 같은 검사를 다시 실행한다. 검수 스크립트 삭제·완화·우회, 빌드 명령 변경, 실패한 상태의 push는 금지한다.

## 5. 작업 종료

1. 위 A·B·C 경로에서 요구한 검수만 수행한다.
2. 검수 실패는 발행 중단 신호다. 원인을 수정한 뒤 실패한 검사와 그 뒤 단계만 다시 실행한다. 검수 스크립트 삭제·완화·우회, 빌드 명령 변경, 실패한 상태의 push는 금지한다.
3. 운영 상태·절차·다음 작업이 달라졌을 때만 `HANDOFF.md`의 현재 실행 브리프와 최신 기록을 갱신한다. 단순 문구 수정은 HANDOFF에 누적하지 않는다.
4. 발행 요청이면 한국어 커밋 메시지로 요청 범위의 파일만 커밋해 `main`에 push한다.
5. Cloudflare Pages(https://bbinge-fc.pages.dev)의 `deploy.json` SHA와 대상 결과를 확인한다. 같은 값의 확인을 여러 도메인·여러 도구로 중복하지 않는다.

## 개발 서버

Claude Code에서 개발 서버는 백그라운드 모드로 실행한다:

```
astro dev --background
```

관리: `astro dev stop`, `astro dev status`, `astro dev logs`
