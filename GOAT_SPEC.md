# GOAT 토너먼트 사양 v1.0

확정일: 2026-07-28 · 운영자 승인 사항: D1 확장, 136명 풀 + 매판 랜덤 32강 (v0.3 명단 확정)

## 0. 승인된 브리프 이탈

브리프 §2 "백엔드 없음" 원칙에 대해, 브리프가 예고한 확장 경로(Cloudflare Workers/Functions + D1)를 투표 통계·게시판에 한해 조기 실행한다. 운영자 승인: 2026-07-28. 정적 사이트 본체는 그대로이며, D1 장애·한도 초과 시에도 게임 자체는 동작해야 한다(통계 기록만 실패 허용).

## 1. 게임 구조

- 선수 풀 136명(`src/data/goat-players.json`, 운영자 확정 v0.3). 매 판 시작 시 시대별 안배 랜덤 추첨으로 32명을 뽑아 대진표를 만든다.
- 시대 구간과 추첨 인원(합 32)은 데이터 파일의 `drawAllocation`을 따른다: 1950년대 이전 3 / 1950–60년대 7 / 1970–80년대 7 / 1990–2000년대 10 / 2010년대–현역 5.
- 라운드: 32강 → 16강 → 8강 → 4강 → 결승. 매 경기 두 카드 중 하나를 선택. 진행 상태는 브라우저 메모리(+새로고침 대비 localStorage)에만 둔다.
- 우승 확정 화면: 우승 선수 카드 크게 + "내 브래킷" 요약 + 결과 공유(URL 파라미터로 우승자·4강 인코딩) + 통계 보기 + (2단계) 한마디 남기기.
- 대진 시드는 URL로 재현 가능하게(`?seed=`) — 친구에게 같은 대진 공유 가능.

## 2. 선수 카드 데이터 스키마

```json
{
  "id": "puskas-ferenc",
  "name": "페렌츠 푸스카스",
  "nameOriginal": "Puskás Ferenc",
  "nationality": "헝가리",
  "clubs": ["혼베드", "레알 마드리드"],
  "position": "FW",
  "era": "1950s",
  "nickname": "Öcsi",
  "nicknameKr": "꼬마 동생",
  "note": ""
}
```

- `position`: GK/DF/MF/FW 4분류. `era`: pre1950 / 1950s60s / 1970s80s / 1990s00s / 2010s 5분류.
- 카드 표기: 이름(굵게) / 국적 · 포지션 / 주요 클럽 / 별명 원어 + (한국어 해석). 통산 기록 나열 금지 — 서사가 카드의 얼굴이다.
- 명단 v0.3(136명)는 운영자가 확정했다(2026-07-28). 이제 명단·별명·표기는 확정 카피이며, 수정은 운영자 지시로만 한다. 별명이 빈 문자열인 선수는 카드에서 별명 대신 note를 노출한다.

## 3. 투표 통계 (구현 완료: 2026-08-16)

- 기록 시점: 우승 확정 순간 `POST /api/goat/result`를 백그라운드에서 한 번 호출한다. 경기 중에는 네트워크 요청을 보내지 않는다.
- 저장 구조: `tournament_results`에 세션·시드·우승자·준우승자·4강 진출자·완주 시각을 저장하고, `matchup_votes`에 31경기의 라운드·대진·선택 결과를 정규화해 저장한다.
- 검증: 서버가 32강부터 결승까지 정확히 31경기인지, 각 라운드 참가자와 승자가 앞 라운드 결과에 맞게 이어지는지 검증한다. 변조되거나 알 수 없는 선수 id는 거부한다.
- 중복 방지: 브라우저에서 만든 익명 세션 id와 시드 조합은 한 번만 기록한다. IP·회원 계정은 저장하지 않는다.
- 랭킹 페이지 `/play/ranking/`: 총 완주 수, 우승 횟수·우승률, 1대1 승수·승률, 결승·4강 진출 횟수를 136명 전원 기준으로 제공한다. API는 `GET /api/goat/ranking`이며 5분 캐시를 사용한다.
- 실패 허용: D1 또는 기록 API가 실패해도 게임과 결과 화면은 정상 완료되며 통계·댓글만 비활성화된다.

## 4. 완주 댓글 (구현 완료: 2026-08-16)

- 완주자만 작성할 수 있다. 서버가 `resultId`와 동일 익명 세션의 완주 기록을 확인한 뒤 댓글을 저장한다.
- 닉네임은 20자, 본문은 150자까지다. 본문 URL은 최대 1개이며 같은 세션은 30초에 한 번만 작성할 수 있다.
- 최신 댓글은 `GET /api/goat/comments`로 최대 50개까지 읽고, 작성은 `POST /api/goat/comments`를 사용한다. 브라우저 출력은 항상 텍스트로 처리해 HTML을 실행하지 않는다.
- 운영자 삭제는 `DELETE /api/goat/comments/:id`와 Cloudflare 환경변수 `GOAT_ADMIN_TOKEN`을 사용한다. 토큰을 설정하지 않으면 삭제 API는 비활성 상태다.

## 5. 기술 원칙

- 게임 화면은 `/play/` 전용 번들. 글 페이지에 이 스크립트가 실리지 않게 분리한다(브리프 §3-2).
- D1 스키마·Function 코드는 저장소에 포함하되, DB 바인딩·관리 토큰 등 비밀값은 Cloudflare 대시보드에만.
- 디자인은 DESIGN.md 토큰을 따른다. 선수 카드 선택 상태·승자 표시에만 블루 사용.
