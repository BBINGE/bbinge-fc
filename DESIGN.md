# BBinge FC 디자인 명세 v1.0

확정일: 2026-07-28 · 시안 승인: 운영자

방향: 토스·삼쩜삼 계열의 절제된 핀테크 감성 + 고밀도 블로그·매거진. 흰 바탕, 유채색은 블루 하나, 위계는 크기·굵기·회색 단계로만 만든다. 여백으로 고급스러움을 내는 사이트가 아니라, 한 화면에 콘텐츠가 많이 보이는 사이트다.

## 1. 색 — 절대 규칙

모든 색은 `src/styles/global.css`의 `--bbfc-*` 변수만 사용한다. 새 hex 값을 컴포넌트에 직접 쓰지 않는다.

- 유채색은 `--bbfc-blue` 하나다. 쓰는 곳: 카테고리 라벨, CTA 버튼, 링크 호버, 선택 상태. 그 외 전부 무채색.
- 블루 남용 금지: 한 화면(뷰포트)에 진한 블루 면(`--bbfc-blue` 배경)은 CTA 1개가 원칙.
- 기존 `src/config/categories.ts`의 카테고리별 색(coral/teal/purple/pink)은 폐기한다. 카테고리 구분은 색이 아니라 라벨 텍스트로 한다. `CategoryColor` 타입과 관련 매핑을 제거하고 사용처를 정리할 것.
- 다크모드는 v1 범위 아님.

## 2. 타이포그래피

- Pretendard Variable 단일 서체. 세리프 없음.
- 전역 자간 -0.01em (global.css에 적용됨).
- 굵기는 400(본문)과 500(제목·강조) 두 단계만. 600 이상 금지.
- 크기 단계 (px): 페이지 제목 24 / 대표글 제목 17~18 / 리스트 제목 13.5~14 / 본문 15~16 / 보조 13 / 메타 12. 이 단계 밖의 임의 크기를 만들지 않는다.
- **화면에 보이는 글자는 12px 미만을 쓰지 않는다**(운영자 결정, 2026-09-21). 배지·영문 라벨·기록 카드 하단·층 안내까지 예외 없다. 경위와 검수는 §9.
- 본문 행간 1.7, 제목 행간 1.3~1.4.

## 3. 레이아웃 밀도 원칙

- 첫 화면(스크롤 전)에 글 8개 이상 + 도구 진입 1개가 보여야 한다.
- 콘텐츠 최대 폭 1040px, 중앙 정렬. 모바일 좌우 패딩 16px, 데스크톱 20px.
- 카드보다 리스트를 우선한다. 리스트 행 구분은 `--bbfc-line` 1px 가는 선.
- 섹션 사이 간격 24~32px. 그 이상의 큰 여백을 만들지 않는다.

## 4. 페이지별 구성

### 메인 (`/`)

위에서부터:

1. 헤더: 좌측 로고 `BBinge FC`(FC만 블루) + 카테고리 텍스트 메뉴(역사·순례·놀이·컬쳐), 우측 검색 아이콘. 높이 56px, 하단 `--bbfc-line` 구분.
2. GOAT 배너: `--bbfc-blue-weak` 배경의 낮은 가로 배너. 좌측에 라벨(`PLAY · 32강`, 블루 12px) + 제목("당신의 GOAT는 누구인가요?") + 한 줄 설명, 우측에 블루 CTA "시작하기". 히어로처럼 크게 만들지 않는다 — 세로 90px 내외.
3. 대표글 + 리스트 2단: 좌측 1.35fr 대표글(썸네일 16:10, 카테고리 태그, 제목 17px, 요약 2줄, 메타), 우측 1fr 텍스트 리스트 4건(카테고리 라벨 12px 블루 + 제목 13.5px, 행 사이 `--bbfc-line`). 모바일에서는 세로로 쌓는다.
4. 최신 글 3열 그리드: 썸네일 + 제목 12.5px + 메타. 모바일 1열.
5. 푸터: 사이트명, about·privacy·contact 링크, 저작권. 최소 높이.

### 카테고리 목록 (`/[category]/`)

- 상단: 카테고리명 24px + 글 수.
- 본문: 썸네일 좌측 120px + 제목/요약/메타 우측의 가로형 리스트 행 반복. 페이지네이션은 "더 보기" 없이 전체 출력(정적 사이트, 글 수백 편까지 문제 없음. 그 이상이 되면 연도별 분할 검토).

### 글 상세 (`/[category]/[slug]/`)

- 본문 최대 폭 680px 중앙(가독 폭). 제목 24px, 메타(카테고리·날짜·읽는 시간), 본문 16px/1.7.
- 본문 요소: h2는 18px/500 + 상단 여백 32px, 인용은 좌측 3px 블루 보더, 코드·표는 `--bbfc-fill` 배경.
- 본문 하단: 같은 카테고리 최신 글 3건 리스트 + AdSlot 자리.

### 공통

- AdSlot: 회색 점선 보더의 자리표시 유지(승인 전). 위치: 글 상세 본문 하단, 카테고리 목록 중간(5번째 행 뒤).
- 이미지 없음 글의 썸네일: `--bbfc-fill` 단색 + 카테고리 라벨 텍스트로 대체(임의 그라데이션 금지).

## 5. 구현·검증

- 컴포넌트 수정 범위: Layout, Header, Footer, CategoryNav, ArticleCard, ArticleGrid, MagazineShell, Sidebar(사용 여부 재검토 — 밀도 원칙상 메인 2단 구성에 흡수되면 제거).
- 검증: `npm run build` 성공, 380px·768px·1280px 3구간, 첫 화면 글 8개 규칙 충족 확인.
- 이 명세와 충돌하는 판단이 필요하면 임의로 진행하지 말고 질문한다.

## 6. 강조 막대 금지 — "손톱" (운영자 지시, 2026-09-18)

**블록이나 칸의 한 변에만 붙는 2px 이상 유채색 단색 막대를 만들지 않는다.** 사이트 전체에서 77곳을 걷어냈고, 본문 `h2`의 파란 왼쪽 막대와 그 들여쓰기도 이때 없앴다. 소제목은 본문과 같은 선에서 시작한다.

운영자 판단의 근거는 "사람들이 손톱 디자인 보면 AI 디자인이라 싫어한대"이다. 이 사이트의 존재 이유가 AI가 쓸 수 없는 글인데 껍데기가 AI 티를 내면 안 된다.

### 손톱이 아닌 것 (남긴다)

- 머리글 아래 전체 폭 무채색 괘선(각 관 masthead, 아카이브 절 구분 등 21곳)
- 표의 행 구분선, 칸 사이 1px 구분선
- 경기장 도해선, 막대그래프 막대, 범례 색칸
- 연표의 세로 레일(점을 얹는 축이라 걷으면 점이 뜬다)
- 마우스를 올릴 때 늘어나는 밑줄, 스크롤에 맞춰 늘어나는 전체 폭 밑줄

### 작은 판도 같은 금기다

표를 만들 때 칸 옆에 붙이는 3px 세로 막대, 소제목 위에 까는 26px짜리 색 대시가 여기 해당한다. 운영자 표현으로 "클로드 특유의 쪼마난 손톱같은 버릇, 표 만들 때"다.

**정보를 나르는 표시가 필요하면 막대가 아니라 칸 자체를 옅게 물들이거나 글자색을 바꾼다.** 해축 순위표의 진출권 표시를 3px 세로 막대에서 순위 칸 30% 색 채움으로 바꾼 것이 기준 사례다.

### 블록을 구분해야 할 때

막대 대신 짙은 브랜드 블록을 쓴다(운영자 승인 규격, 축디 9편·축술 4편·축세 8곳에 적용).

- 135도 브랜드 그라데이션, 반지름 18px, 테두리 0
- 구단 문장이나 SVG 워터마크를 불투명도 12~15%로 배경에 깐다
- 라벨은 구단 보조색 + 인라인 SVG 아이콘, 본문은 흰색 84~88%
- 860px 또는 767px 이하에서 축소판을 둔다

### 점검 방법

강조 막대를 셀 때 **`border-left`만 세지 말고 네 변을 다 센다.** 한 번 `border-left`만 세어 31건이라 보고했다가 실제로는 132건이었고 운영자가 직접 짚었다. 가상요소(`::before`/`::after`)로 만든 얇은 막대도 함께 센다.

막대를 뗄 때 배경이 없는 블록은 **막대 자리를 비우려 넣었던 들여쓰기도 함께 걷는다.** 안 그러면 이유 없는 여백만 남는다.

## 7. 인용문 규격 (운영자 확정, 2026-09-18)

본문 인용은 막대도 받침도 없이 **여백과 글자색으로만** 구분한다. 규칙은 `src/pages/[...path].astro`의 전역 `blockquote`에 있다.

순서는 이렇다.

1. **번역문** — 큰 활자, 잉크색
2. **원어** — 보조 활자, 회색 (라틴 문자에는 Georgia)
3. **누가 어디서 언제 왜** — `<cite>`, 앞에 긴 대시

뒤 두 줄은 갖추지 않아도 된다. 번역문만 있는 인용이 대부분이다.

운영자가 보여 준 네이버 블로그 예시 인용구에는 막대선이 있지만, 그건 네이버 템플릿이라 그런 것이다. **우리 사이트에 막대를 되살리라는 뜻이 아니다.**

예외: 축쿼드의 신격화 배너(`atleti-quote`·`barca-quote`·`athletic-invocation`·`gerd-invocation`·`heynckes-invocation`)와 UEFA 명판, 남미 편 맺음은 태그가 `<blockquote>`일 뿐 디자인 블록이다. 평평하게 만들지 않는다.

## 8. 이미지와 글 사이의 여백 (운영자 지시, 2026-09-19)

운영자가 "사이트 전 칼럼 전체적으로 이미지와 글씨가 너무 좁게 붙어 있다, 이미지 그림자가 글을 덮고 있다"고 짚었다. 원고마다 붙인 드롭섀도가 아래로 최대 47px 번지는데 블록의 아래 여백이 22-48px뿐이어서 그림자가 글자 위로 올라와 있었다. 실제로 유벤투스 블록코어 편은 그림자가 글을 25px 덮고 있었다.

규칙은 `src/pages/[...path].astro`의 전역 `.article-body`에 있다. 이미지나 영상을 품은 블록 바로 다음에 오는 글에 **바닥값**을 준다.

| 블록 | 다음 글까지 바닥값 |
|---|---|
| `figure`·`table`·`ul` | `clamp(56px, 5.5vw, 80px)` |
| 전면 패널 `section`·`div`·`aside` | `clamp(88px, 7.5vw, 112px)` |

전면 패널을 더 띄우는 까닭은 그쪽에만 큰 드롭섀도가 달리기 때문이다. 어느 화면에서도 그림자가 끝난 자리부터 글자까지 40px 넘게 남는다(380px에서 최소 41px, 1440px에서 최소 61px).

삥이 아카이브(`.archive-body`)도 같은 값을 쓴다. 다만 구단 문장 같은 작은 아이콘이 들어간 기록 그리드와 그 아래 설명(`.record-note`)은 원래 간격을 지킨다. 사진이 아니라 표이기 때문이다.

대회 기록표도 같은 이유로 전면 패널 값에서 뺀다(운영자 지시, 2026-09-22, "너무 붙지도 않고 너무 떨어지지도 않게"). 결승 H/L의 누적 표(`.cup-stage-scroll`)는 칸에 문장 아이콘이 있어 전면 패널로 잡혀, 제목에서 표까지 32px인데 표에서 다음 소제목까지 122-146px이 벌어져 있었다. 지금은 표 아래 62px(표 여백 34px + 다음 글 28px)이고, 절이 바뀌는 `h2`만 기본 간격을 쓴다. 아카이브의 누적 표(`.cup-entrants`)는 자체 아래 여백 48px을 쓴다. 대진 카드(`.cup-tie`)처럼 어두운 패널은 이 예외에 넣지 않는다.

**바닥값이라 이미 더 넓게 띄운 블록은 제 값을 지킨다.** 원고에서 여백을 더 주는 것은 막지 않는다. 반대로 이 값을 낮춰 예전처럼 붙이지 않는다.

3절의 고밀도 원칙은 목록·카드·메인 화면의 규칙이다. **글 본문 안의 이미지 여백에는 적용하지 않는다.**

### 글 문단 길이

한 문단이 화면을 가득 채우면 읽기 어렵다. 레버쿠젠 편의 1432자 한 문단을 운영자 지시로 다섯 문단(216-312자)으로 나눴다. 나눌 때 **문장은 한 글자도 고치지 않고 줄바꿈만 넣는다.** 출처 표시가 끝나는 자리가 대개 화제가 바뀌는 자리다.

## 9. 글자 하한 12px (운영자 결정, 2026-09-21)

운영자가 축쿼드 목록 카드의 `커스텀 베스트 11`·`글 읽기 →`(10px)와 층 안내의 `CURRENT FLOOR`·`07F`·`FLOOR MAP`(8px, 모바일 7.5px)을 보여주며 "인간 눈에 잘 안 보인다"고 짚었다. 227개 페이지를 1280px로 띄워 재 보니 화면에 보이는 12px 미만 글자가 8,643곳이었고(11px 3,636 · 10px 2,627 · 8px 851 · 9~9.5px 879 등), 가장 작은 것은 7.2px였다. 소스에서는 95개 파일, 736개 선언이었다. 대부분 자간을 넓힌 영문 대문자 라벨(킥커·아이브로), `NEW` 배지, 날짜·메타였다.

**하한은 12px이다.** 운영자가 12px과 13px 가운데 12px을 골랐다. 13px로 올리면 메타와 보조(13)의 위계가 사라지고 첫 화면 8편 원칙이 흔들린다.

- 소스의 `font-size: Npx`, Tailwind `text-[Npx]`, `font: … Npx/…`는 12px 미만을 쓰지 않는다. rem은 `.75rem`이 하한이다.
- em 단위는 부모 크기에 따라 결과가 달라진다. 12px 미만으로 계산되지 않는지 화면에서 확인한다.
- 크기를 올렸을 때 한 줄에 들어가지 않는 영문 라벨은 **자간을 줄이거나 줄바꿈을 허용해서** 맞춘다. 다시 작게 만들어 맞추지 않는다.
- 범위 밖: 운영자의 Photoshop 편집물·카드뉴스 **이미지 속** 글자, SVG 아이콘 안의 로고 글자(`about`의 `blog` 마크).

### 점검 방법

`scripts/validate-min-font-size.mjs`가 빌드 첫머리에서 소스를 검사하고 12px 미만 선언이 있으면 빌드를 멈춘다(`npm run validate:min-font`). 검사 기준을 낮추거나 예외를 추가해 통과시키지 않는다.

em·상속으로 생기는 작은 글자는 소스 검사로 잡히지 않는다. 새 컴포넌트나 인라인 스타일이 많은 원고를 올린 뒤에는 미리보기에서 화면에 보이는 글자의 계산된 `font-size`를 모아 12 미만이 0인지 본다. 380px과 1280px 두 폭에서 본다.
