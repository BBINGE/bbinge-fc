---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { categories, getArticlePath } from '../config/categories';
import { site } from '../config/site';
import { isNewlyPublished } from '../utils/freshness';
import TodayFootball from '../components/TodayFootball.astro';
import { imageFocusStyle } from '../utils/imageFocus';

const articles = (await getCollection('articles', ({ data }) => !data.draft)).map((entry) => ({
  id: entry.id,
  href: getArticlePath(entry.data.category, entry.id),
  title: entry.data.title,
  description: entry.data.description,
  label: categories[entry.data.category]?.label ?? entry.data.category,
  coverImage: entry.data.coverImage,
  coverImageAlt: entry.data.coverImageAlt ?? entry.data.title,
  pubDate: entry.data.pubDate,
  kind: 'article' as const,
}));

const archiveEntries = (await getCollection('archive', ({ data }) => !data.draft)).map((entry) => ({
  id: entry.id,
  href: `/archive/${entry.data.branch}/${entry.data.index}/${entry.id}/`,
  title: entry.data.title,
  description: entry.data.description,
  label: '삥이 아카이브',
  coverImage: entry.data.coverImage,
  coverImageAlt: entry.data.coverImageAlt,
  pubDate: entry.data.pubDate,
  kind: 'archive' as const,
}));

const editorialFeed = [...articles, ...archiveEntries].sort(
  (a, b) => b.pubDate.valueOf() - a.pubDate.valueOf() || b.id.localeCompare(a.id)
);

const FEATURED_ARTICLE_ID = 'hungary-golden-team-total-football-elo-rating';
const lead = editorialFeed.find((item) => item.kind === 'article' && item.id === FEATURED_ARTICLE_ID) ?? editorialFeed[0];
const rollingFeed = editorialFeed.filter((item) => item.href !== lead?.href);
const secondary = rollingFeed.slice(0, 4);
const latest = rollingFeed.slice(4, 12);
const moreStories = rollingFeed.slice(12, 18);
---

<Layout title={site.name} description={site.tagline} breadcrumbs={[{ label: '홈', href: '/' }]}>
  <main class="home-magazine max-w-[1180px] mx-auto px-4 md:px-6 py-7 md:py-10">
    <h1 class="sr-only">삥이FC - 축구의 역사와 문화를 읽고 기록하는 독립 매거진</h1>

    <header class="edition-head">
      <div>
        <p class="edition-kicker">BBINGE FC · FOOTBALL ARCHIVE MAGAZINE</p>
        <p class="edition-title">축구의 역사를 놀고, 읽고, 걷는 곳</p>
      </div>
      <p class="edition-note">역사 · 공간 · 유니폼 · 서사</p>
    </header>

    <nav class="identity-hub" aria-label="삥이FC와 저자 소개">
      <div class="identity-hub__intro">
        <span class="identity-hub__portraits" aria-hidden="true">
          <img class="identity-hub__profile" src="/images/profile-bbinge.png" alt="" width="52" height="52" />
          <img class="identity-hub__mark" src="/images/brand/pb-mark-2026-color.png" alt="" width="44" height="30" />
        </span>
        <div>
          <p>WHO WRITES BBINGE FC?</p>
          <strong>축구를 알기 위해 다른 것들을 읽기 시작한 사람.</strong>
        </div>
      </div>
      <div class="identity-hub__links">
        <a href="/about/">
          <img src="/images/profile-bbinge.png" alt="" width="38" height="38" />
          <span>AUTHOR</span><strong>About 삥이</strong><i aria-hidden="true">→</i>
        </a>
        <a href="/about/logo/">
          <img src="/images/brand/pb-mark-2026-color.png" alt="" width="38" height="38" />
          <span>IDENTITY</span><strong>로고 이야기</strong><i aria-hidden="true">→</i>
        </a>
      </div>
    </nav>

    {lead ? (
      <section class="cover-grid" aria-label="주요 기사">
        <a href={lead.href} class="lead-story group">
          <div class="lead-image media-frame">
            {lead.coverImage ? (
              <img src={lead.coverImage} alt={lead.coverImageAlt} loading="eager" fetchpriority="high" style={imageFocusStyle(lead.coverImage)} />
            ) : (
              <span>이미지 준비 중</span>
            )}
            <span class="story-label story-label-on-image">EDITOR'S SIGNATURE</span>
          </div>
          <div class="lead-copy">
            <div class="story-meta">
              <span>{lead.label}</span>
              <time datetime={lead.pubDate.toISOString()}>{lead.pubDate.toLocaleDateString('ko-KR')}</time>
              {isNewlyPublished(lead.pubDate) && <span class="new-mark">NEW</span>}
            </div>
            <h2>{lead.title}</h2>
            <p>{lead.description}</p>
            <span class="read-more">이야기 읽기 <span aria-hidden="true">→</span></span>
          </div>
        </a>

        <div class="secondary-stack">
          {secondary.map((item, index) => (
            <a href={item.href} class="secondary-story group">
              <div class="secondary-image media-frame">
                {item.coverImage ? (
                  <img src={item.coverImage} alt={item.coverImageAlt} loading="lazy" style={imageFocusStyle(item.coverImage)} />
                ) : (
                  <span>이미지 준비 중</span>
                )}
                <span class="story-number">0{index + 2}</span>
              </div>
              <div class="secondary-copy">
                <div class="story-meta">
                  <span>{item.label}</span>
                  {isNewlyPublished(item.pubDate) && <span class="new-mark">NEW</span>}
                </div>
                <h2>{item.title}</h2>
                <p>{item.description}</p>
              </div>
            </a>
          ))}
        </div>
      </section>
    ) : (
      <p class="empty-state">첫 번째 이야기를 준비하고 있습니다.</p>
    )}

    <TodayFootball />

    <section class="below-fold" aria-labelledby="latest-heading">
      <div class="section-heading">
        <div>
          <p>THE LATEST</p>
          <h2 id="latest-heading">최근 기록</h2>
        </div>
        <a href="/archive/">전체 아카이브 <span aria-hidden="true">→</span></a>
      </div>

      <div class="latest-layout">
        <div class="latest-list">
          {latest.length > 0 ? latest.map((item, index) => (
            <a href={item.href} class="latest-row">
              <span class="latest-index">{String(index + 1).padStart(2, '0')}</span>
              <div class="latest-thumb media-frame">
                {item.coverImage ? <img src={item.coverImage} alt="" loading="lazy" style={imageFocusStyle(item.coverImage)} /> : <span>NO IMAGE</span>}
              </div>
              <div class="latest-copy">
                <div class="story-meta"><span>{item.label}</span></div>
                <h3>{item.title}</h3>
                <p>{item.description}</p>
              </div>
              <time datetime={item.pubDate.toISOString()}>{item.pubDate.toLocaleDateString('ko-KR')}</time>
            </a>
          )) : (
            <p class="empty-latest">다음 기록을 준비하고 있습니다.</p>
          )}
        </div>

        <a href="/play/" class="play-feature" aria-label="GOAT 32강 토너먼트 시작하기">
          <div class="play-feature__copy">
            <p>BBINGE FC PRESENTS</p>
            <strong>GOAT<br />SHOWDOWN</strong>
            <h2>전설 32명.<br />왕은 한 명.</h2>
            <span>32강 바로 시작 <b aria-hidden="true">▶</b></span>
          </div>
          <div class="play-feature__roster" aria-hidden="true">
            <img src="/images/goat/players/di-stefano.webp" alt="" class="goat-face goat-face--1" loading="lazy" decoding="async" />
            <img src="/images/goat/players/puskas.webp" alt="" class="goat-face goat-face--2" loading="lazy" decoding="async" />
            <img src="/images/goat/players/pele.webp" alt="" class="goat-face goat-face--3" loading="lazy" decoding="async" />
            <img src="/images/goat/players/beckenbauer.webp" alt="" class="goat-face goat-face--4" loading="lazy" decoding="async" />
            <img src="/images/goat/players/cruyff.webp" alt="" class="goat-face goat-face--5" loading="lazy" decoding="async" />
            <img src="/images/goat/players/maradona.webp" alt="" class="goat-face goat-face--6" loading="lazy" decoding="async" />
            <img src="/images/goat/players/cristiano.webp" alt="" class="goat-hero goat-hero--ronaldo" loading="lazy" decoding="async" />
            <img src="/images/goat/players/messi.webp" alt="" class="goat-hero goat-hero--messi" loading="lazy" decoding="async" />
          </div>
          <span class="play-feature__eight" aria-hidden="true">32</span>
        </a>
      </div>
    </section>

    {moreStories.length > 0 && (
      <section class="more-records" aria-labelledby="more-records-heading">
        <div class="section-heading">
          <div><p>MORE STORIES</p><h2 id="more-records-heading">기록 더 보기</h2></div>
          <a href="/search/">모든 기록 검색 <span aria-hidden="true">→</span></a>
        </div>
        <div class="more-grid">
          {moreStories.map((item) => (
            <a href={item.href} class="more-card group">
              <div class="more-image media-frame">
                {item.coverImage ? <img src={item.coverImage} alt={item.coverImageAlt} loading="lazy" style={imageFocusStyle(item.coverImage)} /> : <span>NO IMAGE</span>}
              </div>
              <div class="story-meta"><span>{item.label}</span><time datetime={item.pubDate.toISOString()}>{item.pubDate.toLocaleDateString('ko-KR')}</time></div>
              <h3>{item.title}</h3>
              <p>{item.description}</p>
            </a>
          ))}
        </div>
      </section>
    )}
  </main>
</Layout>

<style>
  .home-magazine { color:var(--bbfc-ink); }
  .edition-head { display:flex; align-items:flex-end; justify-content:space-between; gap:24px; padding:0 0 18px; border-bottom:2px solid var(--bbfc-ink); }
  .edition-kicker,.section-heading p { color:var(--bbfc-blue); font-size:15px; line-height:1.4; font-weight:750; letter-spacing:.16em; }
  .edition-title { margin-top:7px; font-size:17px; line-height:1.4; font-weight:620; letter-spacing:-.025em; }
  .edition-note { color:var(--bbfc-gray-2); font-size:15px; letter-spacing:.08em; white-space:nowrap; }
  .identity-hub { display:flex; align-items:stretch; justify-content:space-between; gap:24px; margin-top:18px; padding:12px 13px; border:1px solid var(--bbfc-border); border-radius:var(--bbfc-radius-lg); background:var(--bbfc-fill-2); }
  .identity-hub__intro { display:flex; min-width:0; align-items:center; gap:13px; }
  .identity-hub__portraits { position:relative; display:block; width:76px; height:54px; flex:0 0 auto; }
  .identity-hub__profile { position:absolute; left:0; top:1px; width:52px; height:52px; border:2px solid var(--bbfc-bg); border-radius:50%; object-fit:cover; box-shadow:0 5px 14px -9px rgba(25,31,40,.7); }
  .identity-hub__mark { position:absolute; right:0; bottom:0; width:38px; height:28px; padding:3px; border:1px solid var(--bbfc-border); border-radius:9px; background:var(--bbfc-bg); object-fit:contain; box-shadow:0 5px 14px -9px rgba(25,31,40,.7); }
  .identity-hub__intro p,.identity-hub__links span { color:var(--bbfc-blue); font-size:15px; line-height:1.35; font-weight:750; letter-spacing:.14em; }
  .identity-hub__intro strong { display:block; margin-top:3px; color:var(--bbfc-ink-2); font-size:15px; line-height:1.5; font-weight:570; }
  .identity-hub__links { display:grid; grid-template-columns:repeat(2,minmax(170px,1fr)); gap:9px; }
  .identity-hub__links a { display:grid; grid-template-columns:38px minmax(0,1fr) 25px; grid-template-rows:auto auto; align-items:center; column-gap:10px; min-width:170px; padding:9px 10px; border:1px solid var(--bbfc-border); border-radius:11px; background:var(--bbfc-bg); box-shadow:0 9px 22px -21px rgba(25,31,40,.7); transition:color .18s,background .18s,border-color .18s,transform .18s,box-shadow .18s; }
  .identity-hub__links a:hover { border-color:var(--bbfc-blue); color:var(--bbfc-blue); background:var(--bbfc-blue-weak); transform:translateY(-1px); box-shadow:0 12px 24px -20px rgba(20,91,198,.6); }
  .identity-hub__links a:focus-visible { outline:3px solid var(--bbfc-blue); outline-offset:2px; }
  .identity-hub__links img { grid-column:1; grid-row:1 / 3; width:38px; height:38px; padding:2px; border:1px solid var(--bbfc-border); border-radius:50%; background:#fff; object-fit:cover; }
  .identity-hub__links a:last-child img { padding:5px; object-fit:contain; }
  .identity-hub__links span { grid-column:2; align-self:end; }
  .identity-hub__links strong { grid-column:2; align-self:start; margin-top:2px; font-size:15px; line-height:1.4; font-weight:680; }
  .identity-hub__links i { grid-column:3; grid-row:1 / 3; display:grid; place-items:center; width:25px; height:25px; border-radius:50%; background:var(--bbfc-blue); color:#fff; font-size:15px; font-style:normal; transition:transform .18s; }
  .identity-hub__links a:hover i { transform:translateX(2px); }
  .cover-grid { display:grid; grid-template-columns:minmax(0,1.72fr) minmax(300px,.88fr); gap:30px; padding:28px 0 42px; border-bottom:1px solid var(--bbfc-border); }
  .lead-story { display:grid; grid-template-rows:auto 1fr; min-width:0; }
  .media-frame { position:relative; overflow:hidden; background:var(--bbfc-fill); }
  .media-frame img { display:block; width:100%; height:100%; object-fit:cover; transition:transform .45s ease; }
  .group:hover .media-frame img { transform:scale(1.025); }
  .media-frame>span:not(.story-label):not(.story-number) { display:grid; place-items:center; min-height:100%; color:var(--bbfc-gray-2); font-size:15px; }
  .lead-image { aspect-ratio:16/9; }
  .story-label-on-image { position:absolute; left:0; bottom:0; padding:8px 12px; background:var(--bbfc-blue); color:white; font-size:15px; font-weight:750; letter-spacing:.13em; }
  .lead-copy { padding-top:20px; }
  .story-meta { display:flex; align-items:center; gap:9px; color:var(--bbfc-gray-2); font-size:15px; line-height:1.4; }
  .story-meta>span:first-child { color:var(--bbfc-blue); font-weight:700; }
  .new-mark { padding:3px 5px; border-radius:999px; background:var(--bbfc-blue-weak); color:var(--bbfc-blue)!important; font-size:15px; font-weight:800; letter-spacing:.06em; }
  .lead-copy h2 { max-width:760px; margin-top:10px; font-size:clamp(29px,3.25vw,45px); line-height:1.16; font-weight:720; letter-spacing:-.055em; text-wrap:balance; }
  .lead-copy>p { max-width:740px; margin-top:13px; color:var(--bbfc-gray-1); font-size:15px; line-height:1.75; }
  .read-more { display:inline-block; margin-top:19px; color:var(--bbfc-ink); font-size:15px; font-weight:650; border-bottom:1px solid var(--bbfc-ink); padding-bottom:3px; }
  .secondary-stack { display:grid; align-content:start; gap:0; min-width:0; }
  .secondary-story { display:grid; grid-template-columns:122px minmax(0,1fr); gap:15px; padding:18px 0; border-bottom:1px solid var(--bbfc-border); }
  .secondary-story:first-child { padding-top:0; }
  .secondary-story:last-child { padding-bottom:0; border-bottom:0; }
  .secondary-image { aspect-ratio:4/3; align-self:start; }
  .story-number { position:absolute; top:7px; left:7px; padding:4px 6px; background:rgba(25,31,40,.78); color:#fff; font-size:15px; font-weight:750; letter-spacing:.08em; }
  .secondary-copy h2 { margin-top:7px; font-size:15.5px; line-height:1.38; font-weight:680; letter-spacing:-.035em; }
  .secondary-copy>p { margin-top:6px; color:var(--bbfc-gray-1); font-size: 15px; line-height:1.55; display:-webkit-box; -webkit-line-clamp:2; -webkit-box-orient:vertical; overflow:hidden; }
  .below-fold { padding-top:38px; }
  .section-heading { display:flex; align-items:flex-end; justify-content:space-between; gap:20px; padding-bottom:14px; border-bottom:1px solid var(--bbfc-ink); }
  .section-heading h2 { margin-top:5px; font-size:25px; line-height:1.2; font-weight:680; letter-spacing:-.04em; }
  .section-heading>a { color:var(--bbfc-gray-1); font-size:15px; }
  .latest-layout { display:grid; grid-template-columns:minmax(0,1fr) 300px; gap:34px; padding-top:4px; }
  .latest-row { display:grid; grid-template-columns:28px 112px minmax(0,1fr) auto; align-items:center; gap:15px; padding:16px 0; border-bottom:1px solid var(--bbfc-line); }
  .latest-index { color:var(--bbfc-gray-3); font-family:Georgia,serif; font-size:15px; }
  .latest-thumb { aspect-ratio:4/3; border-radius:2px; }
  .latest-copy h3 { margin-top:5px; font-size:15px; line-height:1.4; font-weight:650; letter-spacing:-.025em; }
  .latest-copy>p { margin-top:5px; color:var(--bbfc-gray-1); font-size: 15px; line-height:1.55; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
  .latest-row>time { align-self:start; padding-top:4px; color:var(--bbfc-gray-3); font-size:15px; white-space:nowrap; }
  .play-feature { position:relative; align-self:start; min-height:0; aspect-ratio:5/11; margin-top:16px; overflow:hidden; border-radius:12px; background:radial-gradient(circle at 72% 22%,#24599a 0,#0d294e 34%,#07182e 72%); color:#fff; isolation:isolate; box-shadow:0 18px 45px -24px rgba(5,20,42,.8); }
  .play-feature::before { content:''; position:absolute; inset:0; z-index:1; background:linear-gradient(180deg,rgba(5,15,29,.05) 25%,rgba(5,15,29,.92) 82%); }
  .play-feature::after { content:''; position:absolute; inset:0; z-index:5; border:1px solid rgba(255,255,255,.16); border-radius:inherit; pointer-events:none; }
  .play-feature__copy { position:absolute; z-index:6; left:21px; right:19px; bottom:22px; }
  .play-feature__copy p { color:#78acff; font-size:15px; font-weight:850; letter-spacing:.18em; }
  .play-feature__copy>strong { display:block; margin-top:5px; color:#fff; font-size:30px; line-height:.86; font-weight:950; font-style:italic; letter-spacing:-.065em; text-shadow:0 3px 18px rgba(0,0,0,.45); }
  .play-feature__copy h2 { margin-top:12px; color:rgba(255,255,255,.94); font-size:15px; line-height:1.35; font-weight:720; letter-spacing:-.035em; }
  .play-feature__copy>span { position:absolute; right:0; bottom:0; display:inline-flex; align-items:center; gap:8px; margin-top:0; padding:10px 14px; border:1px solid rgba(255,255,255,.4); border-radius:999px; background:#2469d7; font-size:15px; font-weight:850; box-shadow:0 8px 24px rgba(3,16,34,.38); backdrop-filter:blur(7px); }
  .play-feature__copy b { font-size:15px; }
  .play-feature__roster { position:absolute; inset:0; z-index:2; }
  .play-feature__roster img { position:absolute; display:block; object-fit:cover; object-position:center top; filter:saturate(.78) contrast(1.08); transition:transform .35s ease,filter .35s ease; }
  .play-feature:hover .play-feature__roster img { filter:saturate(1) contrast(1.08); }
  .goat-face { z-index:4; width:82px; height:98px; border:2px solid rgba(255,255,255,.34); border-radius:10px; box-shadow:0 8px 18px rgba(0,0,0,.3); }
  .goat-face--1 { left:14px; top:14px; transform:rotate(-4deg); }
  .goat-face--2 { left:50%; top:8px; transform:translateX(-50%) rotate(2deg); }
  .goat-face--3 { right:14px; top:14px; transform:rotate(4deg); }
  .goat-face--4 { left:14px; top:96px; transform:rotate(3deg); }
  .goat-face--5 { left:50%; top:102px; transform:translateX(-50%) rotate(-2deg); }
  .goat-face--6 { right:14px; top:96px; transform:rotate(-3deg); }
  .goat-hero { bottom:118px; width:64%; height:348px; object-position:center top; -webkit-mask-image:linear-gradient(to bottom,#000 70%,transparent 100%); mask-image:linear-gradient(to bottom,#000 70%,transparent 100%); }
  .goat-hero--ronaldo { left:-15%; z-index:2; }
  .goat-hero--messi { right:-15%; z-index:1; transform:scale(1.03); }
  .play-feature:hover .goat-hero--ronaldo { transform:translateX(4px) scale(1.02); }
  .play-feature:hover .goat-hero--messi { transform:translateX(-4px) scale(1.05); }
  .play-feature__eight { position:absolute; z-index:0; top:165px; left:50%; color:rgba(88,152,255,.13); font-family:Georgia,serif; font-size:190px; line-height:1; letter-spacing:-.12em; transform:translateX(-53%); }
  .empty-state,.empty-latest { padding:50px 0; color:var(--bbfc-gray-2); font-size:15px; text-align:center; }
  .more-records { margin-top:54px; padding-top:42px; border-top:1px solid var(--bbfc-border); }
  .more-grid { display:grid; grid-template-columns:repeat(3,minmax(0,1fr)); gap:34px 22px; padding-top:22px; }
  .more-card { min-width:0; }
  .more-image { aspect-ratio:16/10; margin-bottom:14px; border-radius:3px; }
  .more-card h3 { margin-top:8px; font-size:17px; line-height:1.4; font-weight:670; letter-spacing:-.035em; }
  .more-card>p { margin-top:7px; color:var(--bbfc-gray-1); font-size: 15px; line-height:1.65; display:-webkit-box; -webkit-line-clamp:2; -webkit-box-orient:vertical; overflow:hidden; }
  .more-card .story-meta { justify-content:space-between; }

  @media (max-width: 900px) {
    .identity-hub { align-items:center; }
    .identity-hub__intro strong { max-width:310px; }
    .cover-grid { grid-template-columns:1fr; }
    .secondary-stack { grid-template-columns:repeat(2,minmax(0,1fr)); gap:22px; }
    .secondary-story { grid-template-columns:1fr; }
    .secondary-story,.secondary-story:first-child { padding:0 0 22px; }
    .secondary-story:nth-last-child(-n+2) { padding-bottom:0; border-bottom:0; }
    .secondary-image { aspect-ratio:16/9; }
    .latest-layout { grid-template-columns:1fr; }
    .play-feature { width:100%; aspect-ratio:16/8.5; margin-top:0; }
    .play-feature__copy { top:auto; right:18px; bottom:18px; width:auto; transform:none; }
    .play-feature__copy>strong { font-size:34px; }
    .play-feature__copy h2 { font-size:15px; }
    .play-feature__roster { left:0; }
    .goat-hero { bottom:-5%; width:54%; height:72%; }
    .goat-hero--ronaldo { left:-2%; }.goat-hero--messi { right:-2%; }
    .goat-face { top:0; width:16.667%; height:34%; border:0; border-radius:0; box-shadow:none; transform:none; }
    .goat-face--1 { left:0; top:0; }.goat-face--2 { left:16.667%; top:0; transform:none; }.goat-face--3 { left:33.334%; right:auto; top:0; }
    .goat-face--4 { left:50%; right:auto; top:0; }.goat-face--5 { left:66.667%; right:auto; top:0; transform:none; }.goat-face--6 { left:83.334%; right:auto; top:0; }
    .play-feature__eight { top:-25px; left:73%; }
    .more-grid { grid-template-columns:repeat(2,minmax(0,1fr)); }
  }
  @media (max-width: 600px) {
    .home-magazine { padding-top:20px; }
    .edition-head { align-items:flex-start; }
    .edition-note { display:none; }
    .edition-title { font-size:15px; }
    .identity-hub { display:grid; gap:10px; padding:12px; }
    .identity-hub__portraits { width:68px; height:50px; }
    .identity-hub__profile { width:48px; height:48px; }
    .identity-hub__mark { width:35px; height:26px; }
    .identity-hub__intro strong { font-size: 15px; }
    .identity-hub__links { grid-template-columns:repeat(2,minmax(0,1fr)); }
    .identity-hub__links a { grid-template-columns:32px minmax(0,1fr) 23px; min-width:0; padding:8px; }
    .identity-hub__links img { width:32px; height:32px; }
    .identity-hub__links i { width:23px; height:23px; }
    .cover-grid { gap:28px; padding-top:20px; }
    .lead-copy h2 { font-size:30px; }
    .lead-copy>p { font-size:15px; line-height:1.7; }
    .secondary-stack { grid-template-columns:1fr; gap:20px; }
    .secondary-story { grid-template-columns:118px minmax(0,1fr); padding-bottom:20px; }
    .secondary-story:first-child { padding-top:0; }
    .secondary-story:nth-last-child(-n+2) { padding-bottom:20px; border-bottom:1px solid var(--bbfc-border); }
    .secondary-story:last-child { padding-bottom:0; border-bottom:0; }
    .secondary-image { aspect-ratio:4/3; }
    .secondary-copy h2 { font-size:15px; }
    .secondary-copy>p { -webkit-line-clamp:2; font-size: 15px; }
    .below-fold { padding-top:30px; }
    .latest-row { grid-template-columns:22px 82px minmax(0,1fr); gap:10px; }
    .latest-copy>p,.latest-row>time { display:none; }
    .latest-copy h3 { font-size: 15px; }
    .more-records { margin-top:42px; padding-top:32px; }
    .more-grid { grid-template-columns:1fr; gap:28px; }
    .more-card { display:grid; grid-template-columns:112px minmax(0,1fr); column-gap:13px; align-items:start; }
    .more-image { grid-row:1 / span 3; margin:0; aspect-ratio:4/3; }
    .more-card .story-meta { justify-content:flex-start; }
    .more-card h3 { margin-top:5px; font-size:15px; }
    .more-card>p { margin-top:5px; font-size:15px; }
    .play-feature { aspect-ratio:5/4; border-radius:10px; }
    .play-feature__copy { left:18px; right:18px; bottom:18px; width:auto; }
    .play-feature__copy p { font-size:15px; }
    .play-feature__copy>strong { font-size:28px; }
    .play-feature__copy h2 { display:none; }
    .play-feature__copy>span { right:0; padding:10px 13px; font-size:15px; }
    .play-feature__roster { left:0; }
    .goat-face { width:33.333%; height:29%; border:0; border-radius:0; box-shadow:none; transform:none; }
    .goat-face--1 { left:0; top:0; }.goat-face--2 { left:33.333%; top:0; transform:none; }.goat-face--3 { left:66.666%; right:auto; top:0; }
    .goat-face--4 { left:0; right:auto; top:29%; }.goat-face--5 { left:33.333%; right:auto; top:29%; transform:none; }.goat-face--6 { left:66.666%; right:auto; top:29%; }
    .goat-hero { bottom:-3%; width:58%; height:58%; -webkit-mask-image:linear-gradient(to bottom,#000 82%,transparent 100%); mask-image:linear-gradient(to bottom,#000 82%,transparent 100%); }
    .goat-hero--ronaldo { left:-5%; z-index:3; }
    .goat-hero--messi { right:-5%; z-index:2; }
    .play-feature::before { z-index:4; background:linear-gradient(180deg,rgba(5,15,29,0) 40%,rgba(5,15,29,.12) 55%,rgba(5,15,29,.96) 100%),linear-gradient(90deg,rgba(5,17,33,.82) 0,rgba(5,17,33,.15) 48%,rgba(5,17,33,.08) 100%); }
    .play-feature__eight { z-index:1; opacity:.05; }
  }
  @media (prefers-reduced-motion: reduce) {
    .media-frame img { transition:none; }
    .group:hover .media-frame img { transform:none; }
  }
</style>
