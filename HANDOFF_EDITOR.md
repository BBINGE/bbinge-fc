# bbinge-fc — 인수인계문 (2026-07-30 갱신)

> 이 문서는 새 채팅 세션 시작 시 **관제탑(Claude 본체)**이 맥락을 그대로 이어받기 위한 인수인계문이다. 어제 문서(2026-07-29)를 대체한다.

---

## 0. 협업 구조 — 먼저 읽을 것

### 관제탑 ↔ 실무자 구조
- **관제탑 = Claude 본체 (지금 이 채팅)**: 진단·판단·검수·승인·브리프 작성. 사용자와 직접 대화하며 실무자에게 내릴 지시를 정리한다. 코드를 직접 배포하거나 커밋하지 않는다.
- **실무자 = Claude Code**: 관제탑이 내린 지시를 실제 코드/커밋/배포로 실행. GitHub API로 실물 검증까지 한다.
- 사용자는 관제탑의 지시를 실무자에게 전달하고, 실무자의 답변을 다시 관제탑에게 가져와 검수받는 **크로스 검증 루프**로 일한다.

### 관제탑이 지켜온 원칙 (어제 세션에서 확립)
1. **표면 증상 ≠ 근본 원인.** 어제 진단이 두 번 뒤집혔다 — "팝업 차단"의 실체는 OAuth 구조 문제였고, "웹훅 누락/연동 끊김"의 실체는 그냥 꺼진 자동배포 스위치였다. 증상만 보고 캐시 삭제·disconnect 같은 파괴적 조치를 하지 말고, **실제 데이터(콘솔 에러, Check Run 이력, 대시보드 상태)로 원인을 다시 짚을 것.**
2. **승인 순서를 지킨다.** 구조 변경(여러 파일 동시 수정)은 브랜치/커밋으로 롤백 여지 확보 후 진행. 단, 영향 범위가 확실히 격리된 단일 파일 변경은 main 직행 허용.
3. **공유 엔드포인트 주의.** `/auth`·`/callback`은 에디터(write)와 Sveltia CMS(`/admin`)가 공유한다. 여기 건드릴 땐 항상 CMS 회귀 테스트를 먼저 돌린다.
4. **브라우저 "성공" ≠ 실제 커밋.** 발행/업로드 후엔 GitHub API로 실물 커밋 해시까지 확인한다.

### 사용자의 7툴 분업 (참고)
- ChatGPT: 본업/부업/글쓰기 — Google Notebook: 개인 연구·논문 분석 — Notion: 논문·포트폴리오 아카이빙 — Gemini: 가족 관련 업무
- ChatGPT Codex: 디어한의원 홈페이지 등 회사/개인 사이트 (지금 열일 중, 방해 금지)
- **Claude 본체(관제탑) + Claude Code(실무자): 부업 = bbinge-fc.** 이 라인이 이 문서의 대상.

---

## 1. 어제(07-29) → 오늘(07-30) 사이 완료된 3건

### ✅ (1) 에디터 OAuth: 팝업 → 전체 리다이렉트 전환
- **문제:** `/admin/write/`에서 사진·발행 시 "팝업이 차단되었습니다" 메시지가 시크릿 창·팝업 허용 상태에서도 계속 떴다. 원인은 크롬 설정이 아니라 `window.open` 기반 OAuth 구조 자체.
- **해결:** 팝업 방식을 제거하고 **전체 페이지 리다이렉트** 방식으로 전환.
  - `functions/auth.js`: `flow=write` 쿼리 파라미터를 쿠키(`oauth_flow`)에 저장
  - `functions/callback.js`: `flow=write`면 `/admin/write/#gh_token=...`(성공)/`#gh_auth_error=...`(실패)로 302 리다이렉트. `flow` 없는 기본 요청(CMS 팝업+postMessage)은 그대로 유지 — **분기 처리로 CMS 로그인 회귀 없음 확인 완료.**
  - `src/pages/admin/write.astro`: `window.open` 제거. 로그인 필요 시 초고 자동저장 후 `/auth?flow=write`로 페이지 전체 이동. 복귀 시 URL 해시에서 토큰 읽어 `localStorage` 저장, `history.replaceState`로 해시 즉시 제거, 자동저장 초고 복원.
- **토큰 저장:** `localStorage` 유지. (GitHub classic OAuth 토큰은 자체 만료 없음 → sessionStorage 전환해도 보안 이득 없고 재로그인 불편만 커짐. 관리자 전용 noindex 페이지라 XSS 표면 낮음.)
- **검증:** CMS 로그인 정상 / 사진 업로드 완료 / 해시 제거·초고 복원 확인.

### ✅ (2) 재발행 422 (sha 누락) 버그 수정
- **문제:** 첫 발행은 성공하나, 같은 slug 재발행 시 `PUT .../articles/{slug}.md → 422` + "Invalid request. sha wasn't supplied". GitHub Contents API는 기존 파일 업데이트 시 현재 `sha`를 요구하는데 `commitFile`이 신규 생성만 가정했음.
- **해결:** PUT 전 GET으로 경로 존재 확인 → 있으면 sha를 받아 PUT body에 포함, 404면 신규 생성. `commitFile`은 발행+이미지 업로드 공용 함수라 양쪽 동시 해결. 커밋 `2ade516` (main 직행 — CMS는 unpkg 별개 번들이라 이 함수 안 씀, 영향 격리 확인).

### ✅ (3) Cloudflare 자동 배포 복구
- **문제:** 매 발행마다 GitHub Actions "Scheduled deploy" 수동 실행 필요. 어제 문서는 "연동 끊김", 실무자는 "간헐적 웹훅 누락"으로 진단 → **둘 다 오진.**
- **진짜 원인:** Cloudflare 대시보드 → Workers & Pages → bbinge-fc → Settings → **Branch control**의 "Automatic deployments"가 **Disabled**였음. 그래서 모든 push가 "Skipped"로 찍혔고, 성공한 배포는 전부 Scheduled deploy 훅 수동 호출분이었음.
- **해결:** Branch control에서 "Enable automatic production branch deployments" 체크 → Save. (Production branch: main)
- **실증 검증:** 빈 커밋 `c8131f2` push → 8초 내 Cloudflare 네이티브 빌드 "Building" 시작(워크플로 훅 아님) → `Deployed successfully`. 자동 배포 정상 작동 확인.
- **안전망 유지:** `.github/workflows/scheduled-deploy.yml`에 `on: push: branches:[main]` 트리거 추가해둠. 네이티브 자동배포가 또 꺼지거나 웹훅 누락 시 이중 안전망. 중복 빌드는 발행 빈도상 무시 수준.

### 현재 상태
- **에디터 실전 투입 가능.** 사진 업로드·발행·재발행·자동배포 전부 정상.
- 테스트로 만든 글 존재: slug `3141`, `423423423` 등 (전부 숫자 slug, 비공개/draft). 실제 콘텐츠 넣을 때 정리 or 무시. **slug는 발행 후 변경 금지**(URL 깨짐), 한글 제목은 영문·숫자만 추출되므로 의미 있는 영문 slug로 직접 지정 권장.

---

## 2. 오늘부터 할 일 — 매거진 UI 구현 (확정 방향)

어제 콘텐츠·UI 방향을 사용자와 확정함:
- **톤: 블로그/피드형** (가볍고 스크롤 편한 카드 나열)
- **1순위: UI 매거진 틀** (레이아웃·카드·타이포)
- **콘텐츠 채우기: UI 틀 나온 뒤** (순수 피드만으론 1,052편+ 축구 아카이브 탐색이 약하므로, 피드 카드에 카테고리 태그·필터 훅을 걸어 아카이브 탐색 여지를 남긴다)

### 실무자(Claude Code)에게 내릴 브리프 초안
> **[bbinge-fc 매거진 UI 구현 — 피드형]**
> 목표: 홈/카테고리 페이지를 가볍고 스크롤 편한 카드 피드형 매거진으로 구현. Astro 유지.
> 1. **카드 컴포넌트**: 썸네일(표준 비율 1개 고정, `object-fit:cover`)·제목·발췌·카테고리 태그·발행일. `src/content/articles` 프론트매터 자동 매핑.
> 2. **피드 레이아웃**: 반응형 그리드(데스크톱 다단→모바일 1단). 우선 단순 나열 or 페이지네이션.
> 3. **카테고리 필터**: 상단 축세/축행/축겜/축디 탭. `categories.json` 단일 소스. (아카이브 탐색 대비 훅)
> 4. **타이포/컬러 토큰**: bbinge-fc 톤(축구 아카이브다운 절제된 무드) 정의.
> 5. **draft(비공개) 글은 피드에서 제외.**
> 제약: 기존 발행 파이프라인(write.astro·자동배포) 안 건드림, 순수 프론트 레이아웃. 레이아웃 대공사이므로 브랜치 생성 후 작업. 먼저 카드 1개+피드 1페이지 프로토타입 → 톤 확인 → 필터·반응형 확장.

### 새 세션 시작 시 관제탑이 사용자에게 먼저 확인할 것 (어제 미해결)
1. **썸네일 표준 비율** — 과거 재활용 이미지가 가로형(16:9)/세로형/정사각 중 다수가 무엇인가? 카드 비율 고정 기준.
2. **프로토타입 목업** — 실무자에게 넘기기 전 관제탑이 피드 카드 레이아웃을 시각적으로 먼저 그려 톤 확인할지 여부.

---

## 3. 이후 백로그 (UI 확정 후 순차)
1. **콘텐츠 채우기 워크플로**: 과거 축구글 재활용·업셀링 + 신규 칼럼. 재활용 이미지 활용. (UI 틀 나온 뒤 설계)
2. **뉴스레터 기획안** (신규) — 코드 의존 낮으므로 관제탑과 병렬 기획 가능.
3. **게임 사이트 "게임화"** — GOAT 토너먼트 등. BBINGE_FC_BRIEF.md의 GOAT 툴에 얹는 구조. 별도 트랙.
4. **유튜브 임베드** (에디터 3단계, 계속 보류 중, 우선순위 낮음).

---

## 4. 배포·운영 레퍼런스
- **사이트:** https://bbinge-fc.pages.dev  ·  **에디터:** https://bbinge-fc.pages.dev/admin/write/  ·  **CMS:** https://bbinge-fc.pages.dev/admin/
- **저장소:** github.com/BBINGE/bbinge-fc (프레임워크: Astro)
- **자동 배포: 이제 정상.** main push 시 Cloudflare 네이티브 자동 빌드. (수동 배포 필요 시: Actions → "Scheduled deploy" → Run workflow → main)
- **이미지:** R2 폐기, GitHub `public/images/` 폴더 방식. 업로드 직후 라이브에서 엑박이면 **버그 아님** — 배포 반영 전일 뿐. 자동배포 켜졌으니 잠시 후 반영됨.
- **카테고리:** `src/data/categories.json` 단일 소스. `/admin`에서 추가·수정·삭제. (축세·축행·축겜·축디)

## 5. 주요 커밋 (07-29~30)
- `ff59d6f` — OAuth 팝업→리다이렉트 전환 (main 머지)
- `2ade516` — 재발행 sha 버그 수정
- `c8131f2` — 자동배포 검증용 빈 커밋 (네이티브 자동배포 실증)
- `scheduled-deploy.yml` — push 트리거 안전망 추가

## 6. 다음 세션 시작 지점
1. 이 문서로 관제탑 맥락 복원 → 협업 구조(§0) 확인
2. 매거진 UI 착수: 사용자에게 §2의 확인 2건(썸네일 비율·목업 여부) 질문
3. 답 나오면 → 카드+피드 프로토타입 브리프를 실무자(Claude Code)에게 전달
4. 실무자 결과 → 관제탑 검수 → 톤 OK 시 필터·반응형 확장
