# bbinge-fc 에디터 — 인수인계문 (2026-07-29)

## 한 줄 요약
에디터 6단계 중 **5단계까지 코드·배포 완료**. 배포된 `/admin/write/` 화면에서 카테고리 선택·발행 패널·자동저장 전부 정상 확인됨. **마지막 실전 검증(사진 삽입·발행) 딱 하나만 남았고, 그것도 코드 문제가 아니라 크롬 팝업 차단 하나 때문에 막혀 있는 상태.**

---

## 에디터 6단계 진행 현황

| 단계 | 내용 | 상태 |
|------|------|------|
| 1단계 | 위지윅 본문 편집(TipTap) | ✅ 완료·검증 |
| 2단계 | 이미지 저장 | **R2 → GitHub `public/images` 폴더 방식으로 대체 확정** (카드 문제로 R2 포기, 폴더 방식이 백업·비용 면에서 오히려 유리) |
| 3단계 | 유튜브 임베드 | ⬜ 미착수 (우선순위 낮음, 맨 뒤로) |
| 4단계 | 카테고리 데이터화 | ✅ 완료·배포·검증 (`src/data/categories.json` 단일 소스, `/admin`에서 추가·수정·삭제. 축세·축행·축겜·축디 마이그레이션 유지) |
| 5단계 | 자동저장 + 발행 패널 | ✅ 코드 완료·배포 완료. 화면에 카테고리 선택·slug 자동생성·설명·태그·발행일(예약)·비공개(초안) 체크박스·0.8초 자동저장 전부 확인됨 |

---

## 지금 막혀 있는 딱 하나: 크롬 팝업 차단

- 발행/사진 삽입은 **GitHub 로그인 팝업**을 띄워서 진행되는데, 크롬이 이 팝업을 차단 중.
- 화면 상단 빨간 글씨: "팝업이 차단되었습니다. 브라우저에서 팝업을 허용한 뒤 다시 시도하세요."
- **이건 코드 문제가 아님.** 팝업을 띄우려 시도한다는 것 자체가 로그인 연동이 살아있다는 증거.

### 해결 순서 (새 세션에서 이것부터)
1. 새 탭 주소창에 `chrome://settings/content/popups` 입력
2. "허용됨" 섹션 → **추가** → `https://bbinge-fc.pages.dev` 등록
3. 기존 `/admin/write/` 탭 완전히 닫고(자동저장돼 있어 내용 복구됨) 새 탭에서 다시 접속
4. **사진 버튼** 클릭 → GitHub 로그인 창 뜨는지 확인
5. 안 되면 시크릿 창(Ctrl+Shift+N)에서 테스트 → 확장프로그램 간섭 여부 판별

### 실전 테스트 체크리스트 (팝업 풀린 뒤)
- [ ] 사진 버튼 → GitHub 로그인 → 이미지가 실제 `public/images/`에 커밋되는지
- [ ] 발행 버튼 → `src/content/articles/{slug}.md` 커밋되는지
- [ ] **비공개(초안) 체크는 반드시 유지** (테스트 글이 사이트에 안 뜨게)
- 통과하면 → 에디터 검증 완전 종료

---

## 배포 관련: 반드시 기억할 이슈 (미해결)

**GitHub → Cloudflare Pages 자동 배포 연동이 현재 끊겨 있음.**
- 5단계 커밋(`6adc493`) push했는데 Cloudflare 빌드가 아예 트리거 안 됐음(웹훅 유실이 아니라 연동 자체 끊김으로 판단). 빈 커밋 재push도 무반응.
- **임시 우회책으로 GitHub Actions "Scheduled deploy" 워크플로를 수동 실행**해서 배포 성공(#73). 이 예약배포 워크플로가 Cloudflare 배포 훅을 직접 호출하는 구조라 안전망이 됨.
- **근본 원인(자동 연동 끊김)은 아직 안 고침.** 이대로면 글 발행할 때마다 매번 Actions 수동 실행 필요.
- 이건 Cloudflare 대시보드/GitHub 앱 설정 영역이라 운영자가 직접 봐야 함. **에디터 검증 끝난 뒤 별도로 잡을 것.**

### 배포가 필요할 때 (자동 연동 복구 전까지)
`github.com/BBINGE/bbinge-fc/actions` → "Scheduled deploy" → Run workflow → 1~2분 후 녹색 체크 → Cloudflare 재빌드

---

## 주요 커밋
- `bab3bdf` — 4단계(카테고리 데이터화)
- `6adc493` — 5단계(발행 패널)
- `4441184` — 재배포 트리거용 빈 커밋
- Scheduled deploy #73 — 수동 배포 성공

---

## 다음 세션 시작 지점
1. **크롬 팝업 차단 해제** → 사진·발행 실전 테스트 (위 체크리스트)
2. 통과 후 → GitHub→Cloudflare **자동 배포 연동 복구** (Cloudflare 대시보드)
3. 그 다음 → 3단계(유튜브 임베드) 또는 콘텐츠 채우기(15편 아티클)
4. (보류 중 아이디어) 검증 끝난 에디터를 코덱스에 넘겨 **디어한의원 에디터**도 같은 친절한 UX로 개선

## slug 주의사항
- slug는 URL이라 **발행 후 변경 금지**. 바꾸면 그 글 URL이 깨짐.
- 한글 제목이면 slug가 영문·숫자만 따서 생성됨(예: 제목 "3141한국어…" → slug `3141`). 실제 글은 의미 있는 영문 slug로 직접 수정 권장.
