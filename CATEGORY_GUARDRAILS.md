# 삥이FC — 관별 작업 점검표

이 문서는 **무엇을 쓸지가 아니라 무엇을 확인할지**를 적는다. 편집 의도와 문체는 `CONTENT_EDITORIAL_INTENT.md` §4와 `EDITORIAL_WRITING_RULES.md`가 담당하고, 여기에는 Codex와 Claude Code가 실제로 놓쳐서 사고가 났던 항목만 남긴다. 새 항목은 실제로 한 번 틀린 뒤에 추가한다. 가정이나 모범 사례를 미리 적지 않는다.

---

## 0. 전 카테고리 공통

### 0-1. 측정하고 보고한다

**항목이 비어 있다는 것과 기능이 망가졌다는 것은 다르다.** 둘을 같은 단어로 보고하지 않는다.

2026-09-17 하루에 같은 실수를 네 번 했다. 폭이 다른 블록 목록을 의도 확인 없이 결함으로 올렸고, `llms.txt`와 `citation` 스키마가 없다는 이유로 GEO에 구멍이 났다고 했으며(크롤러로 받아 보니 본문 7,785자가 그대로 읽혔다), `llms.txt`의 효과를 확인 없이 단정했고(구글은 공식적으로 불필요하다고 밝혔다), width/height 없는 이미지 1,009개를 근거로 CLS를 1순위 결함이라 했다(실측 결과 0.0000, 이동 0건).

- 결함이라 부르기 전에 **그 결함의 증상을 직접 잰다.** CLS는 `PerformanceObserver`로, 크롤 가능성은 크롤러 UA로 받아서, 효과는 근거를 찾아서 확인한다.
- 값이 이상하면 **소스를 열어 누가 왜 그렇게 썼는지** 본다. 클래스 이름을 믿지 말고 실제 값을 잰다. `portrait-figure`가 1.78·1.92 가로 사진을 담고 있었다.
- 사실 / 통설 / 추측을 말로 구분한다.
- **"이상 없음"을 부실한 보고로 여기지 않는다.** 고칠 게 둘뿐이면 둘만 적는다.
- 고쳤는데 측정값이 그대로면 "고쳤다"가 아니라 "명시했다"고 적는다.

### 0-2. 출처는 직접 열어 본 것만 쓴다

검색 결과 요약이나 기억으로 URL을 적지 않는다. 호나우두 편에서 그럴듯한 주소 네 개를 지어내 전부 죽어 있었다. 발행 전 반드시 전 출처의 응답 코드를 확인하고, 200이 아니면 교체하거나 뺀다.

**출처 목록이 위키 나열처럼 보이면 형식을 고친다.** 2026-09-18 아틀레티코 편에서 운영자가 "죄다 위키 뭐 봤다라서 복붙한 사이트처럼 보인다"고 지적했다. 세어 보니 열네 줄 가운데 열두 줄이 `Wikipedia`로 시작하고 전부 "무엇을 확인했다"로 끝나, 독자가 읽는 목록이 아니라 작업 일지였다. **주제를 앞세우고 자료명을 뒤로 돌린다**(`구단 연혁과 통산 기록 — 스페인어 위키백과 「…」`). 항목 설명은 한 줄로 줄이고, RSSSF 같은 기록 원장을 찾아 1차 자료를 섞는다. 구단 공식 사이트는 Cloudflare 봇 차단이 흔하며 우회하지 않는다.

```bash
grep -o 'href="https://[^"]*"' 원고.md | sed 's/href="//;s/"//' | sort -u | while read u; do echo "$(curl -s -o /dev/null -w '%{http_code}' -L "$u") $u"; done
```

### 0-3. 네이버 원문은 frontmatter에만

원문 주소는 `priorPublication`에만 둔다. 공개 참고 자료 목록에 넣지 않는다. `validate-editorial-writing.mjs`의 `NAVER-LINK-FRONTMATTER-ONLY`가 빌드에서 막는다.

### 0-4. 폭과 표지

- 머리말·사진·표는 본문 칸의 좌우 끝을 그대로 쓴다. 제목은 칸 끝까지 가는데 설명만 안쪽에서 끝나면 하다 만 화면으로 보인다.
- 표지는 **1:1**이다. 목록 카드가 전부 1:1이라 길쭉한 표지는 잘린다. 글자가 박힌 표지는 특히 확인한다.
- 생사진(운영자가 디자인하지 않은 사진)을 1:1로 자를 때 얼굴이나 주제가 어긋나면 `src/data/image-focal-points.json`에 초점을 등록한다. 실제로 잘라 보고 정한다.
- 세로로 긴 잡지 스캔과 인물 세로컷은 예외로 폭을 제한한다.

### 0-5. 제목은 검색어부터

자동완성을 실제로 받아 확인한다. 추측하지 않는다. 구글 한국어 자동완성 응답은 **EUC-KR**이라 그냥 읽으면 깨진다.

```
https://suggestqueries.google.com/complete/search?client=firefox&hl=ko&q=...
https://ac.search.naver.com/nx/ac?q=...&st=100&r_format=json&r_enc=UTF-8&q_enc=UTF-8
```

검색어가 앞, 훅이 뒤다. 검색 결과에서 잘려도 앞 30자 안에 검색어가 남아야 한다. 대구를 맞춘 추상적 제목은 읽는 사람에게 아무것도 알려 주지 않는다.

### 0-6. 빌드가 막는 것들

실패하면 원고를 고친다. 검사 완화나 예외 추가로 통과시키지 않는다.

| 검사 | 막는 것 |
| --- | --- |
| `validate-editorial-writing` | 제작 과정 노출, 비표준 대회명·인명, 대비형 소제목, 연결 없는 짧은 문장 연타, 네이버 링크, 축세 미리보기 누락 |
| `validate-player-archives` | 인물관 마크업 뼈대, 등번호·수상 기준값 |
| `validate-squad-structure` | 축쿼드 구조 |
| `validate-popup-names` | 선수·구단 표기 |
| `validate-built-markdown` | raw HTML 블록 안에서 변환되지 않은 마크다운 링크 |
| `test-football-ties` | 대진·누적·득점 순위 검산 |

### 0-7. 손이 자주 미끄러지는 곳

- **raw HTML 안에서는 마크다운이 변환되지 않는다.** `<aside>`, `<p class="record-note">`, `<figcaption>` 안에서는 `[글자](/주소)`가 그대로 노출된다. `<a href>`를 쓴다.
- **CRLF.** 이 저장소에는 CRLF 파일이 섞여 있다. `\n---\n` 같은 정규식은 `\r?\n`으로 쓴다. 자바스크립트 정규식에서 `.`는 `\r`를 먹지 않으므로 `^import .*\n`은 CRLF에서 실패한다.
- **셸의 백틱.** `node -e "..."` 안에 백틱을 넣으면 bash가 먼저 실행한다. 문서나 원고를 쓸 때는 스크립트 파일로 만들어 실행한다.
- **원고의 `<img>`에는 실제 파일 크기를 넣는다.** `node scripts/add-image-dimensions.mjs`

### 0-8. 자산

운영자 자산이 먼저다. AI가 표지나 카드뉴스를 새로 만들지 않는다. 이전 GPT가 임의로 만든 표지가 남아 있는 편이 있으므로, 표지를 다룰 때는 그것이 운영자 자산인지 먼저 확인한다. 판단은 `EDITORIAL_ASSET_POLICY.md`와 `EDITORIAL_RISK_GATE.md`를 따른다.

### 0-9. 발행의 끝

`npm run build` 성공만으로 끝내지 않는다. push 뒤 `https://bbinge-fc.pages.dev/deploy.json`의 커밋 해시가 방금 올린 것과 같아질 때까지 확인하고, 라이브 주소와 이미지 응답까지 본다.

---

## 1. 축세 · 6층 세계사관

- **`previewTitle`과 `previewDescription`이 없으면 빌드가 멈춘다.** 상세 제목을 그대로 복사해도 막힌다. 카드에서 읽을 이유를 따로 쓴다(12~42자 / 35~110자).
- 문헌고찰 편은 **각 주장이 틀렸다면 어떤 모습일지를 먼저 적는다.** 반증 조건 없이 시작하면 자기 결론에 유리한 사료만 모으게 된다.
- **증언과 문서 기록을 구분해 표시한다.** 인터뷰에서 나온 말을 문서로 확인된 사실처럼 쓰지 않는다.
- 사료가 갈리면 하나로 합치지 말고 표로 드러내고, 어느 쪽을 따랐는지와 그 이유를 적는다.
- 블록 CSS는 각 원고의 `<style>`에 들어 있다. 새 편에서 `history-thesis`·`history-argument`·`history-takeaway`·`history-timeline`을 쓰려면 기존 편에서 해당 규칙을 옮겨 와야 한다. 옮기지 않으면 스타일 없이 렌더된다.
- `history-argument`의 행은 `<div>`가 아니라 **`<article>`**이어야 그리드가 잡힌다.
- 무거운 주제라도 수감자·희생자의 얼굴이 담긴 기록 사진은 쓰지 않는다. 헤이젤 명판 캡션이 선례다.

## 2. 축행 · 4층

- 제목은 `{도시} 축구 여행`·`{선수} 직관 여행`처럼 실제 검색어 형태로 쓴다(운영자 지시, 2026-09-15).
- 제목이 약속한 정보가 본문에 실제로 있어야 한다. 동선, 문 여는 시간, 표 구하는 법, 경기 뒤 걸을 거리.
- 이 관은 `faq`가 11편 중 10편에 들어가 있다. 새 편에도 넣는다.

## 3. 축디 · 2층 명품관 / 3층 오뭐입?·유니폼관

- 여성 주도 기획이며 구매 가능성과 매력이 판단 기준이다. 안전하게 중립적으로 다듬지 않는다.
- **원고 자체 `<style>`로 폭 리듬을 짜는 편이 있다.** 율레 브란트 편은 1040px 그리드와 560px 단독 컷을 번갈아 둔다. 폭이 제각각이라고 일괄로 맞추지 않는다.
- 표지와 카드는 운영자 정사각 편집물이다.

## 4. 축술 · 7층 전술관

- `pt-*` 블록(`pt-scope`·`pt-caution`·`pt-hypotheses`·`pt-verdicts` 등)은 축술 전용이다. 다른 관에서 클래스만 가져다 쓰지 않는다. 축세에는 `history-*`가 있다.
- 논문·원문을 다룰 때는 원문 도판을 `자료 N` 번호와 함께 인용하고, 번역이 아니라 필자의 판정을 낸다.

## 5. 축떡 · 7층 축떡관

- `faq` frontmatter가 그대로 FAQPage 구조화 데이터로 나간다. 원고가 실제로 답한 것만 적는다.
- `질문 → 판정을 가른 단서 → 짧은 답` 흐름을 지킨다. 규정명 강의로 시작하지 않는다.
- **경기장 도판의 움직임은 경기장 크기에 비례시킨다(2026-09-21 사고).** 페널티킥 편 킥 장면에서 골문은 `%`로 그려 폭에 따라 줄어드는데 공 `pkBall`과 키퍼 `pkKeeper`는 `+82px`·`+70px`로 고정돼 있어, 폭 600px 이하에서 공이 오른쪽 골대를 26px까지 지나쳐 골문 밖에 멈췄다. PC에서는 정상이라 눈으로 잡히지 않았다. 도판 틀에 `container-type:size`를 걸고 이동 거리를 `cqw`·`cqh`로 적어 고쳤다(PC 좌표는 픽셀 단위까지 그대로). 반대로 도판 속 대상은 고정 px로 두고 공만 %로 움직여도 같은 사고가 난다. **점검:** 미리보기에서 애니메이션을 멈춰 놓고(`document.getAnimations()`, `currentTime`) 360·430·768·1280px에서 공의 끝자리가 골문·선수와 같은 관계에 있는지 좌표로 비교한다. `prefers-reduced-motion`의 정지 자세도 같은 단위로 맞춘다.

## 6. 축쿼드 · 8층 명예의 전당

- `SQUAD_ARCHIVE_RULES.md`를 처음부터 끝까지 읽는다. 계열 구분·마크업 규격·숫자 문법·출처 앵커가 모두 규정돼 있다.
- **새 편을 쓰기 전에 같은 계열의 최근 발행 편을 열어 마크업을 눈으로 대조한다.** 규칙 문서만 읽고 쓰지 않는다.
- 표지는 운영자 썸네일이다. AI가 선수 카드나 표지를 만들지 않는다.
- **운영자 원고를 받은 편은 원고 문장 생존율을 재고 발행한다.** 2026-09-18 아틀레티코 편 첫 발행본은 원고 128문장 가운데 23%만 남고 나머지가 위키 사실로 덮여 있었다. 원인은 운영자 문체(`가히`·`무려`·`~했었다`·키워드 나열·감탄)를 AI 문체로 오인해 걷어낸 것이다. **걷어낼 것은 AI가 새로 지어낸 멋부림이지 운영자 문장이 아니다.** 원고를 뼈대로 깔고 확인한 기록을 사이에 끼우는 순서를 지킨다.
- **본문 약칭을 정식명으로 갈아치우지 않는다.** `EDITORIAL_WRITING_RULES.md`의 `CLUB-SHORT-NAME`이 `알레띠`·`바이언`·`보루센`을 확정했다. 같은 편에서 본문 48곳을 정식명으로 바꿔 글이 남의 말투로 읽힌 적이 있다.
- **교정 근거를 본문에 쓰지 않는다.** "운영자 카드에도 없다", "원고가 꼽은", "여기서 숫자 하나를 갱신해 둘 필요가 있는데"는 전부 `NO-PROCESS-META` 위반이다. 근거는 출처 항목에만 남긴다.
- **편집물에 성만 적힌 인물의 원어를 추정해 채우지 않는다.** 아틀레티코 편 서브 명단 골키퍼의 원어를 `José Ignacio`로 지어냈는데 실제는 `Edgardo Mario Madinabeytia Bassi`였다.
- **편집물에 성이나 이니셜만 적힌 자리를 AI가 채울 때가 이 관의 사고 지점이다.** 2026-09-18 하루에 여섯 건이 나왔고 전부 AI가 만든 것이다. 운영자 편집물의 성·이니셜은 대조한 범위에서 틀린 것이 없었다.
  - `José Ignacio Madinabeytia` — 성만 있는 자리에 이름을 지어냈다. 실제는 `Edgardo Mario Madinabeytia Bassi`(1932-2002, 아르헨티나)이며 1962년 구단 최초의 유럽 우승 골키퍼다.
  - `Genar Andrinúa` — 없는 강세를 붙였다. 표제 표기는 `Genar Andrinua Kortabarria`다.
  - `한스게오르크 놀렌더스` — 편집물의 `H.볼레르스`를 잘못 읽었다. 실제는 `호르스트 볼레르스`(Horst Wohlers, 1949-)이며 위키백과에 검색해도 안 나오는 이름이 발행돼 있었다.
  - `카를 콜리크` — 편집물의 `K.쿨리크`를 잘못 읽었다. 실제는 `크리스티안 쿨리크`(Christian Kulik, 1952-)다.
  - `울리히 르 페브르` — 덴마크 이름을 독일식으로 읽었다. `울리크 르 페브르`(Ulrik le Fevre, 1946-2024)다.
  - `Siegfried Held` — 라틴 철자에 `e`를 더 붙였다. 독일어 위키백과에 그 표제어는 문서가 없고 `Sigfried Held`로 넘긴다.
  **확인 순서:** ① 편집물 이미지를 열어 그 자리의 성·이니셜을 눈으로 읽는다 ② 그 성으로 위키백과를 검색해 구단 소속이 맞는지 본다 ③ 검색에 안 나오면 이름이 틀린 것이지 자료가 없는 것이 아니다 ④ 그래도 확인이 안 되면 비워 둔다. 추정해서 채우지 않는다.
- **한글 표기는 AI가 "고치려" 들지 않는다.** 같은 날 `오바므양`을 `오바메양`으로, `미키타리안`을 `므히타랸`으로 바꾸려다 둘 다 운영자가 막았다. 상세는 `CLAUDE.md` 표기 규칙에 있다. 원어 발음 직관보다 국립국어원 용례와 통용 표기가 먼저다.
## 7. H/L · 9층 영화관

- 영상 클립과 본문 주장이 정확히 맞아야 한다. 보이지 않는 장면을 설명하지 않는다.
- 스코어·시간·전환점을 복원한다. 요약하지 않는다.

## 8. 삥이 아카이브 — 인물관

- `PLAYER_ARCHIVE_RULES.md` §2-1의 마크업 뼈대 표를 그대로 따른다. 11개 블록의 순서가 정해져 있다.
- **새 편을 쓰기 전에 최근 발행 편을 열어 마크업을 눈으로 대조한다.** 규칙 문서만 읽고 쓰면 표가 깨진다. 실제로 깨진 적이 있다.
- `record-facts`는 `<div>`이고 국가 수식어가 붙는다. `<dd>` 안에 맨 `<span>`을 쓰지 않는다.
- `club-career-grid`에는 구단 문장이 들어간다. 없으면 이름이 단어 중간에서 줄바꿈된다.
- 발행 검수는 `npm run test:player-rules`, `npm run validate:players`, `npm run build`를 처음부터 다시 돌린다.

## 9. 삥이 아카이브 — 대회관·시상관

- `ARCHIVE_SERIES_RULES.md`를 읽는다.
- **표와 대진은 원고에 손으로 쓰지 않는다.** `src/data`의 JSON과 `render-football-ties.mjs`가 만든다. 득점 순위는 `european-cup-scorers.json` + `data-european-cup-scorers` 자리표시로 낸다. 자체 시험이 합계와 순위를 검산한다.
- 출전 기록이 남지 않은 대회는 출전 칸을 만들지 않는다. 일부만 아는 부분 데이터는 빌드가 막는다.
- 대회 명칭은 시기를 따른다. `유러피언컵` / `UEFA 챔피언스 리그`, `UEFA컵`, `인터시티스 페어스컵`, `인터콘티넨털컵`.
- 새 편에는 `faq`를 넣는다. 원고가 실제로 답한 것만 적는다.
- 분관 id는 `legends` / `leagues` / `club` / `national-team` / `awards` 다섯이다. 남미·아시아 클럽 대항전도 `club` 안에 색인만 추가한다.

## 10. 팝업관 · 1층

- 화면에 기간 한정 문구를 쓰지 않는다. 지난 팝업도 닫지 않고 상시 열어 둔다.
- 목록 라벨은 `POP-UP 01 · 2026.09 개관` 회차 형식이다.
- 로비 줄이 아니라 최근 입고 칸에 일반 글과 같은 카드로 들어간다.

## 11. 축겜 · 5층 오락실

- `GOAT_SPEC.md`를 읽는다.
- D1이 실패해도 사이트와 게임이 동작해야 한다.

---

## 12. 이 문서를 고치는 법

실제로 한 번 틀린 뒤에 그 항목을 추가한다. 무엇을 잘못했고 어떻게 확인했는지를 함께 적는다. 앞으로 조심하겠다는 문장은 적지 않는다. 확인 방법이 없는 항목은 규칙이 아니라 취향이므로 `CONTENT_EDITORIAL_INTENT.md`로 보낸다.
